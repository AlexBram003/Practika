---
title: "ЗВІТ З НАВЧАЛЬНОЇ ПРАКТИКИ"
geometry: "left=2cm,right=1cm,top=2cm,bottom=2cm"
fontsize: 14pt
mainfont: "Times New Roman"
documentclass: article
linestretch: 1.5
indent: true
---

\begin{titlepage}
\centering

{\Large ОЛЕКСАНДРІЙСЬКИЙ ПОЛІТЕХНІЧНИЙ ФАХОВИЙ КОЛЕДЖ}

\vspace{1cm}

Циклова комісія комп'ютерних дисциплін

\vspace{3cm}

{\Large\textbf{ЗВІТ}}

\vspace{0.5cm}

з навчальної практики

\vspace{0.5cm}

з розробки WEB-застосунків

\vspace{0.5cm}

НП 121.242.000

\vspace{2cm}

\begin{flushleft}
Студента ІV курсу ОП-221 групи

Освітньо-професійної програми

«Інженерія програмного забезпечення»

Олександра БРАМА

\vspace{1cm}

Керівники викладачі спецдисциплін,

Едуард БОЙКО

Андрій КОРНІЄНКО

\vspace{1cm}

Оцінка \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
\end{flushleft}

\vfill

м. Олександрія

2025 рік

\end{titlepage}

\newpage

# ЗМІСТ

1. ВСТУП ........................................ 3
2. ОПИС ПРОЄКТУ ................................ 4
3. РЕАЛІЗОВАНІ ТЕМИ ПРАКТИКИ ................... 6
   - 3.1 Створення та налаштування WEB-проєкту .. 6
   - 3.2 Робота з DOM-деревом ................... 8
   - 3.3 Обробка подій користувача .............. 10
   - 3.4 Робота з масивами та об'єктами ......... 12
   - 3.5 Розробка HTML-форм та валідація ........ 14
   - 3.6 Використання Bootstrap та Axios ........ 16
   - 3.7 Взаємодія з зовнішніми API ............. 18
   - 3.8 Обробка помилок у JavaScript ........... 20
   - 3.9 Пагінація та нескінченний скрол ....... 22
   - 3.10 Використання localStorage ............. 24
4. ФУНКЦІОНАЛЬНІ МОЖЛИВОСТІ .................... 26
5. СТРУКТУРА ПРОЄКТУ ........................... 30
6. ІНСТРУКЦІЯ З ВИКОРИСТАННЯ ................... 32
7. ВИСНОВКИ .................................... 34
8. СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ .................. 35

\newpage

# 1. ВСТУП

Метою навчальної практики є закріплення та поглиблення знань, отриманих під час теоретичного навчання, застосування необхідних вмінь та навичок у розробці веб-застосунків.

В рамках практики було обрано **Варіант 6: Photo Gallery** – веб-застосунок для відображення добірки фотографій з Unsplash API із можливістю перегляду в повному розмірі.

## Завдання практики

Основними завданнями навчальної практики є:

- Оволодіння сучасними методами та інструментами розробки вебзастосунків, зокрема роботою з HTML, CSS, JavaScript та системами контролю версій.

- Закріплення та розвиток знань і вмінь, отриманих студентами під час теоретичного навчання, шляхом виконання практичного проєкту.

- Набуття навичок проєктування, створення та тестування клієнтських вебзастосунків.

- Виховання відповідальності за результати своєї роботи та вміння працювати самостійно, використовуючи системи керування проєктами.

- Формування цифрової культури праці, дбайливого ставлення до ресурсів та дотримання правил кібербезпеки.

## Обраний варіант

**Варіант 6: Photo Gallery** – відображати добірку фотографій з API (Unsplash) із можливістю перегляду в повному розмірі.

\newpage

# 2. ОПИС ПРОЄКТУ

**Photo Gallery** – це інтерактивний веб-застосунок для пошуку та перегляду фотографій з безкоштовного API Unsplash.

## Основні характеристики

- **Тип застосунку:** Single Page Application (SPA)
- **API:** Unsplash API
- **Збирач модулів:** Vite 7.2.4
- **Фреймворк UI:** Bootstrap 5.3.8
- **HTTP клієнт:** Axios 1.13.2
- **Мова програмування:** JavaScript ES6+
- **Система контролю версій:** Git

## Функціональність застосунку

1. **Пошук фотографій** за ключовими словами через форму пошуку.

2. **Відображення результатів** у вигляді адаптивної галереї з картками фотографій.

3. **Категорії** – швидкий доступ до популярних тем (Nature, Architecture, Travel тощо).

4. **Сортування** – можливість сортувати фото за популярністю, релевантністю або датою.

5. **Три режими завантаження:**
   - Пагінація з номерами сторінок
   - Кнопка "Завантажити більше"
   - Нескінченний скрол (Infinite Scroll)

6. **Перегляд у повному розмірі** – модальне вікно з детальною інформацією про фото.

7. **Збереження улюблених** – можливість додавати фото в улюблені з використанням localStorage та фільтрувати їх за мінімальною кількістю лайків.

8. **Обробка помилок** – детальні повідомлення про помилки API, мережі та валідації.

**[СКРІН 1: Головна сторінка Photo Gallery]**

*(Має бути зображення головної сторінки з усіма елементами інтерфейсу)*

\newpage

# 3. РЕАЛІЗОВАНІ ТЕМИ ПРАКТИКИ

## 3.1 Створення та налаштування WEB-проєкту

### Завдання

Ініціалізувати веб-проєкт з використанням сучасних інструментів розробки, налаштувати систему збірки, створити Git репозиторій та структуру проєкту.

### Рішення для Photo Gallery

Для проєкту Photo Gallery було обрано Vite як інструмент збірки через його швидкість та підтримку ES6 модулів. Створено проєкт з шаблоном vanilla JavaScript, налаштовано package.json з необхідними залежностями (Bootstrap, Axios), ініціалізовано Git репозиторій та створено модульну структуру файлів.

Проєкт організовано з розділенням відповідальностей: окремі модулі для конфігурації (config.js), стану (state.js), DOM елементів (dom.js), роботи з localStorage (storage.js), API запитів (api.js), UI функцій (ui.js) та обробників подій (events.js).

### Код рішення

Ініціалізація проєкту:

```bash
npm create vite@latest photo-gallery -- --template vanilla
cd photo-gallery
npm install
npm install bootstrap axios
```

Файл package.json:

```json
{
  "name": "photo-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bootstrap": "^5.3.8"
  }
}
```

Git ініціалізація:

```bash
git init
git add .
git commit -m "Початкова ініціалізація проєкту Photo Gallery"
```

Файл .gitignore:

```
node_modules
dist
dist-ssr
*.local
```

**[СКРІН 2: Структура проєкту в VS Code]**

*(Дерево файлів проєкту з папками src/, public/, файлами index.html, package.json, .gitignore)*

\newpage

## 3.2 Робота з DOM-деревом

### Завдання

Навчитися вибирати елементи DOM, динамічно створювати та додавати елементи на сторінку, управляти класами та атрибутами елементів.

### Рішення для Photo Gallery

В Photo Gallery реалізовано систему динамічного створення карток фотографій. Створено централізований об'єкт з посиланнями на всі DOM елементи в файлі dom.js. Картки фотографій створюються динамічно при отриманні даних з API та додаються до галереї через appendChild().

Для показу/приховування елементів використовуються Bootstrap класи через classList API замість inline стилів. Кожна картка містить зображення, інформацію про автора, кількість лайків та кнопку улюбленого.

### Код рішення

Вибір елементів (src/dom.js):

```javascript
import * as bootstrap from 'bootstrap'

export const elements = {
  searchForm: document.getElementById('searchForm'),
  searchInput: document.getElementById('searchInput'),
  photoGallery: document.getElementById('photoGallery'),
  loadingSpinner: document.getElementById('loadingSpinner'),
  errorAlert: document.getElementById('errorAlert'),
  loadMoreBtn: document.getElementById('loadMoreBtn'),
  allTab: document.getElementById('all-tab'),
  favoritesTab: document.getElementById('favorites-tab'),
  photoModal: new bootstrap.Modal(document.getElementById('photoModal')),
  modalPhotoImg: document.getElementById('modalPhotoImg'),
  categoryButtons: document.querySelectorAll('.category-btn')
}
```

Динамічне створення картки фото (src/ui.js):

```javascript
export function createPhotoCard(photo) {
  const col = document.createElement('div')
  col.className = 'col'

  const isFav = isFavorite(photo.id)

  col.innerHTML = `
    <div class="photo-card" data-photo-id="${photo.id}">
      <button class="favorite-btn ${isFav ? 'favorited' : ''}"
              data-photo-id="${photo.id}">
        ${isFav ? '❤️' : '🤍'}
      </button>
      <img src="${photo.urls.small}"
           alt="${photo.alt_description || 'Photo'}"
           loading="lazy" />
      <div class="photo-info">
        <div class="photo-author">📷 ${photo.user.name}</div>
        <div class="photo-likes">❤️ ${photo.likes} вподобань</div>
      </div>
    </div>
  `

  return col
}
```

Додавання до DOM та управління класами:

```javascript
export function displayPhotos(photos, append = false) {
  if (!append) {
    elements.photoGallery.innerHTML = ''
  }

  photos.forEach(photo => {
    const photoCard = createPhotoCard(photo)
    elements.photoGallery.appendChild(photoCard)
  })

  // Управління класами для показу/приховування
  elements.loadMoreBtn.classList.add('d-none')
  elements.scrollHint.classList.remove('d-none')
}
```

**[СКРІН 3: Галерея з динамічно створеними картками фотографій]**

*(9-12 карток фотографій в адаптивній сітці, з кнопками улюблених та інформацією)*

\newpage

## 3.3 Обробка подій користувача

### Завдання

Реалізувати обробку різних типів подій (submit, click, scroll), використати preventDefault(), застосувати делегування подій та debouncing для оптимізації.

### Рішення для Photo Gallery

В Photo Gallery реалізовано комплексну систему обробки подій. Форма пошуку обробляє submit з preventDefault() для запобігання перезавантаження сторінки. Застосовано паттерн делегування подій для обробки кліків на галереї - один обробник на контейнері замість окремих на кожній картці.

Для нескінченного скролу реалізовано обробку події scroll з debouncing (200мс), що запобігає надмірним запитам до API. Додано обробники для перемикання вкладок, вибору категорій, зміни режимів завантаження та фільтрів.

### Код рішення

Обробка submit форми (src/events.js):

```javascript
export function setupSearchForm() {
  elements.searchForm.addEventListener('submit', async (e) => {
    e.preventDefault()

    const query = elements.searchInput.value.trim()

    if (!query) {
      showError('Будь ласка, введіть пошуковий запит')
      return
    }

    state.currentQuery = query
    state.currentPage = 1
    state.currentTab = 'all'

    const photos = await fetchPhotos(query, 1)
    state.photos = photos
    displayPhotos(photos)
  })
}
```

Делегування подій для галереї:

```javascript
export function setupGalleryClick() {
  elements.photoGallery.addEventListener('click', (e) => {
    // Клік на кнопку улюбленого
    if (e.target.closest('.favorite-btn')) {
      const btn = e.target.closest('.favorite-btn')
      const photoId = btn.dataset.photoId

      let photo = state.photos.find(p => p.id === photoId)
      if (photo) {
        toggleFavorite(photo)
        const isFav = isFavorite(photoId)
        btn.textContent = isFav ? '❤️' : '🤍'
        btn.classList.toggle('favorited', isFav)
      }
    }
    // Клік на картку фото
    else if (e.target.closest('.photo-card')) {
      const card = e.target.closest('.photo-card')
      const photoId = card.dataset.photoId
      showPhotoModal(photoId)
    }
  })
}
```

Обробка scroll з debouncing:

```javascript
let scrollTimeout

function handleScroll() {
  clearTimeout(scrollTimeout)

  scrollTimeout = setTimeout(async () => {
    if (isBottomReached() &&
        state.currentTab === 'all' &&
        !state.isLoading &&
        state.loadingMode === 'infinite') {
      await loadMorePhotos()
    }
  }, 200)
}

export function setupInfiniteScroll() {
  window.addEventListener('scroll', handleScroll)
}
```

**[СКРІН 4: Форма пошуку з введеним запитом]**

**[СКРІН 5: Модальне вікно при кліку на фото]**

\newpage

## 3.4 Робота з масивами та об'єктами

### Завдання

Використовувати методи масивів ES6+ (forEach, find, some, filter, map) та оператор spread для роботи з даними.

### Рішення для Photo Gallery

В Photo Gallery активно використовуються методи масивів для обробки колекцій фотографій. forEach() для ітерації та створення карток, find() для пошуку конкретної фотографії за ID, some() для перевірки наявності в улюблених, findIndex() та splice() для видалення з улюблених, filter() для фільтрації за лайками.

Spread оператор використовується для об'єднання масивів при додаванні нових фотографій у режимі infinite scroll, що забезпечує immutable підхід до роботи з даними.

### Код рішення

Метод forEach для відображення фото:

```javascript
photos.forEach(photo => {
  const photoCard = createPhotoCard(photo)
  elements.photoGallery.appendChild(photoCard)
})
```

Метод find для пошуку фото:

```javascript
export function showPhotoModal(photoId) {
  let photo = state.photos.find(p => p.id === photoId)

  if (!photo) {
    const favorites = getFavorites()
    photo = favorites.find(p => p.id === photoId)
  }

  if (!photo) return

  elements.modalPhotoImg.src = photo.urls.regular
  elements.modalPhotoTitle.textContent = photo.alt_description || 'Фото'
  // ...
}
```

Метод some для перевірки улюбленого:

```javascript
export function isFavorite(photoId) {
  const favorites = getFavorites()
  return favorites.some(fav => fav.id === photoId)
}
```

Методи findIndex і splice для видалення:

```javascript
export function toggleFavorite(photo) {
  const favorites = getFavorites()
  const index = favorites.findIndex(fav => fav.id === photo.id)

  if (index > -1) {
    favorites.splice(index, 1)  // Видалити
  } else {
    favorites.push(photo)        // Додати
  }

  saveFavorites(favorites)
}
```

Spread оператор для об'єднання масивів:

```javascript
async function loadMorePhotos() {
  state.currentPage++
  const photos = await fetchPhotos(state.currentQuery, state.currentPage)
  state.photos = [...state.photos, ...photos]
  displayPhotos(photos, true)
}
```

Метод filter для фільтрації:

```javascript
let favorites = getFavorites()

if (state.minLikes > 0) {
  favorites = favorites.filter(photo => photo.likes >= state.minLikes)
}

displayPhotos(favorites)
```

**[СКРІН 6: Вкладка "Улюблені" з фільтрацією]**

\newpage

## 3.5 Розробка HTML-форм та валідація

### Завдання

Створити HTML форми з різними типами полів, реалізувати валідацію через HTML5 атрибути та JavaScript, обробити та використати дані з форм.

### Рішення для Photo Gallery

В Photo Gallery створено дві форми: форма пошуку фотографій та форма фільтрації. Форма пошуку має текстове поле з атрибутом required для базової HTML5 валідації. Додано додаткову JavaScript валідацію через метод trim() для перевірки порожніх значень.

Форма фільтрації містить select для вибору сортування та number input для мінімальних лайків з атрибутами min та step. Реалізовано debouncing для input поля щоб уникнути надмірних запитів при швидкому введенні.

### Код рішення

HTML форма пошуку (index.html):

```html
<form id="searchForm" class="d-flex gap-2">
  <input
    type="text"
    id="searchInput"
    class="form-control"
    placeholder="Введіть ключове слово (наприклад, nature, city, cat...)"
    required
  />
  <button type="submit" class="btn btn-primary">
    Пошук
  </button>
</form>
```

HTML форма фільтрації:

```html
<div class="filters-container mb-3">
  <div class="row g-3">
    <div class="col-md-6">
      <label for="sortOrder" class="form-label small">
        Сортування за популярністю:
      </label>
      <select id="sortOrder" class="form-select form-select-sm">
        <option value="relevant">Релевантність (за замовчуванням)</option>
        <option value="popular">Від найпопулярніших ⬇️</option>
        <option value="latest">Від найновіших 🕒</option>
      </select>
    </div>
    <div class="col-md-6 d-none" id="minLikesContainer">
      <label for="minLikes" class="form-label small">
        Мінімум лайків (тільки для улюблених):
      </label>
      <input type="number" id="minLikes"
             class="form-control form-control-sm"
             placeholder="Наприклад: 100" min="0" step="10">
    </div>
  </div>
</div>
```

JavaScript валідація:

```javascript
const query = elements.searchInput.value.trim()

if (!query) {
  showError('Будь ласка, введіть пошуковий запит')
  return
}
```

Обробка фільтра з debouncing:

```javascript
let likesTimeout

export function setupLikesFilter() {
  elements.minLikesInput.addEventListener('input', () => {
    clearTimeout(likesTimeout)

    likesTimeout = setTimeout(() => {
      const value = parseInt(elements.minLikesInput.value) || 0
      state.minLikes = value

      if (state.currentTab === 'favorites') {
        let favorites = getFavorites()
        if (state.minLikes > 0) {
          favorites = favorites.filter(photo => photo.likes >= state.minLikes)
        }
        displayPhotos(favorites)
      }
    }, 500) // Затримка 500мс
  })
}
```

**[СКРІН 7: Повідомлення про помилку валідації]**

**[СКРІН 8: Форма фільтрації з вибором сортування]**

\newpage

## 3.6 Використання Bootstrap та Axios

### Завдання

Інтегрувати Bootstrap для стилізації UI компонентів, використати Axios для виконання HTTP запитів до зовнішнього API.

### Рішення для Photo Gallery

В Photo Gallery інтегровано Bootstrap 5.3.8 для створення адаптивного інтерфейсу. Використано компоненти: Navbar для навігації, Card для форми пошуку, Modal для перегляду фото у повному розмірі, Tabs для перемикання між "Всі фото" та "Улюблені", Grid system для адаптивної галереї, Form controls для полів введення.

Axios використовується для HTTP запитів до Unsplash API з автоматичним парсингом JSON відповідей та зручною обробкою помилок через promise rejection.

### Код рішення

Імпорт Bootstrap та Axios (src/main.js):

```javascript
import './style.css'
import 'bootstrap/dist/css/bootstrap.min.css'
import * as bootstrap from 'bootstrap'
import axios from 'axios'
```

Bootstrap Navbar (index.html):

```html
<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1">📸 Photo Gallery</span>
  </div>
</nav>
```

Bootstrap Modal:

```html
<div class="modal fade" id="photoModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalPhotoTitle">Фото</h5>
        <button type="button" class="btn-close"
                data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body text-center">
        <img id="modalPhotoImg" src="" class="img-fluid" />
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary"
                data-bs-dismiss="modal">Закрити</button>
        <a id="downloadPhotoBtn" href="" target="_blank"
           class="btn btn-primary">Відкрити оригінал</a>
      </div>
    </div>
  </div>
</div>
```

Ініціалізація модального вікна:

```javascript
export const elements = {
  photoModal: new bootstrap.Modal(document.getElementById('photoModal')),
  // ...
}

// Відкриття модального вікна
elements.photoModal.show()
```

Bootstrap Grid для галереї:

```html
<div id="photoGallery" class="row row-cols-1 row-cols-md-3 g-4"></div>
```

Axios для HTTP запитів (src/api.js):

```javascript
export async function fetchPhotos(query = 'nature', page = 1) {
  try {
    const params = {
      query: query,
      page: page,
      per_page: 12,
      client_id: UNSPLASH_ACCESS_KEY
    }

    if (state.sortOrder !== 'relevant') {
      params.order_by = state.sortOrder
    }

    const response = await axios.get(
      `${UNSPLASH_API_URL}/search/photos`,
      { params }
    )

    return response.data.results
  } catch (error) {
    // Обробка помилок
  }
}
```

**[СКРІН 9: Адаптивний дизайн на різних екранах]**

\newpage

## 3.7 Взаємодія з зовнішніми API

### Завдання

Виконувати HTTP запити до зовнішнього API, обробляти JSON відповіді, використовувати параметри запитів, відображати отримані дані.

### Рішення для Photo Gallery

Photo Gallery інтегровано з Unsplash API для отримання фотографій. Реалізовано асинхронну функцію fetchPhotos() яка відправляє GET запити до endpoint /search/photos з параметрами: query (пошуковий запит), page (номер сторінки), per_page (кількість фото), order_by (сортування), client_id (ключ автентифікації).

API повертає JSON з масивом results, де кожен об'єкт містить дані фотографії: id, urls (посилання на зображення різних розмірів), user (дані автора), likes (кількість вподобань), description (опис). Додано індикатор завантаження під час виконання запиту.

### Код рішення

Конфігурація API (src/config.js):

```javascript
export const UNSPLASH_ACCESS_KEY = 'EyuRTN2cUCiVfSzRT9Us4oc-kHgYsSuqrFrzqvNDumk'
export const UNSPLASH_API_URL = 'https://api.unsplash.com'
```

Асинхронна функція запиту (src/api.js):

```javascript
import axios from 'axios'
import { UNSPLASH_ACCESS_KEY, UNSPLASH_API_URL } from './config.js'
import { state } from './state.js'
import { showLoading, hideError, showError } from './ui.js'

export async function fetchPhotos(query = 'nature', page = 1) {
  try {
    state.isLoading = true
    showLoading(true)
    hideError()

    const params = {
      query: query,
      page: page,
      per_page: 12,
      client_id: UNSPLASH_ACCESS_KEY
    }

    if (state.sortOrder !== 'relevant') {
      params.order_by = state.sortOrder
    }

    const response = await axios.get(
      `${UNSPLASH_API_URL}/search/photos`,
      { params }
    )

    state.isLoading = false
    showLoading(false)

    return response.data.results
  } catch (error) {
    state.isLoading = false
    showLoading(false)
    // Обробка помилок (див. тему 3.8)
    return []
  }
}
```

Використання даних з API:

```javascript
const photos = await fetchPhotos('nature', 1)

photos.forEach(photo => {
  console.log(photo.id)              // "abc123"
  console.log(photo.urls.small)      // "https://images.unsplash.com/..."
  console.log(photo.user.name)       // "John Doe"
  console.log(photo.likes)           // 1234
})
```

Структура JSON відповіді:

```json
{
  "results": [
    {
      "id": "photo-id",
      "urls": {
        "small": "https://...",
        "regular": "https://..."
      },
      "user": {
        "name": "Photographer Name",
        "username": "username"
      },
      "likes": 123,
      "alt_description": "Description"
    }
  ]
}
```

**[СКРІН 10: Індикатор завантаження під час запиту до API]**

**[СКРІН 11: Результати отримані з Unsplash API]**

\newpage

## 3.8 Обробка помилок у JavaScript

### Завдання

Використовувати try-catch блоки, async/await синтаксис, розрізняти типи помилок (мережа, сервер, валідація), відображати зрозумілі повідомлення користувачу.

### Рішення для Photo Gallery

В Photo Gallery реалізовано багаторівневу систему обробки помилок. Всі асинхронні операції обгорнуто в try-catch блоки. Розрізняються три типи помилок від Axios: error.response (сервер відповів з помилкою), error.request (запит відправлено але відповіді немає), інші помилки (проблеми в коді).

Для кожного типу створено специфічне повідомлення українською мовою. Додано обробку помилок localStorage через try-catch. Всі помилки супроводжуються візуальним feedback через Bootstrap alert.

### Код рішення

Try-catch для API запитів:

```javascript
export async function fetchPhotos(query = 'nature', page = 1) {
  try {
    state.isLoading = true
    showLoading(true)

    const response = await axios.get(`${UNSPLASH_API_URL}/search/photos`, {
      params: {
        query, page, per_page: 12,
        client_id: UNSPLASH_ACCESS_KEY
      }
    })

    state.isLoading = false
    showLoading(false)

    return response.data.results
  } catch (error) {
    state.isLoading = false
    showLoading(false)

    // Детальна обробка помилок
    if (error.response) {
      // Сервер відповів з помилкою
      if (error.response.status === 401) {
        showError('Невірний API ключ. Будь ласка, налаштуйте UNSPLASH_ACCESS_KEY в config.js')
      } else if (error.response.status === 403) {
        showError('Перевищено ліміт запитів до API. Спробуйте пізніше.')
      } else {
        showError(`Помилка API: ${error.response.status}`)
      }
    } else if (error.request) {
      // Запит відправлений, але відповіді немає
      showError('Помилка мережі. Перевірте з\'єднання з інтернетом.')
    } else {
      // Інша помилка
      showError('Невідома помилка: ' + error.message)
    }

    return []
  }
}
```

Обробка помилок localStorage:

```javascript
export function getFavorites() {
  try {
    const favorites = localStorage.getItem('favorites')
    return favorites ? JSON.parse(favorites) : []
  } catch (error) {
    console.error('Помилка читання з localStorage:', error)
    return []
  }
}

export function saveFavorites(favorites) {
  try {
    localStorage.setItem('favorites', JSON.stringify(favorites))
    updateFavoritesCount()
  } catch (error) {
    console.error('Помилка запису в localStorage:', error)
    showError('Не вдалося зберегти улюблене фото')
  }
}
```

Відображення помилок (src/ui.js):

```javascript
export function showError(message) {
  elements.errorAlert.textContent = message
  elements.errorAlert.classList.remove('d-none')
}

export function hideError() {
  elements.errorAlert.classList.add('d-none')
}
```

**[СКРІН 12: Повідомлення про помилку мережі]**

**[СКРІН 13: Повідомлення про перевищення ліміту API]**

\newpage

## 3.9 Пагінація та нескінченний скрол

### Завдання

Реалізувати завантаження додаткових даних при прокручуванні сторінки або натисканні кнопки, використати debouncing для оптимізації, відстежувати поточну сторінку.

### Рішення для Photo Gallery

В Photo Gallery реалізовано три режими завантаження: класична пагінація з номерами сторінок, кнопка "Завантажити більше" та infinite scroll.

Пагінація показує інтелектуальні номери сторінок (максимум 5 видимих) з кнопками "Попередня"/"Наступна" та повністю замінює фотографії при зміні сторінки.

Режим "Завантажити більше" додає кнопку яка завантажує наступні 12 фото та додає їх до існуючих.

Infinite scroll автоматично завантажує фото за 300px до кінця сторінки з debouncing (200мс) для запобігання надмірним запитам.

### Код рішення

Функція завантаження додаткових фото:

```javascript
export async function loadMorePhotos() {
  if (state.isLoading || state.currentTab !== 'all') return
  if (state.loadingMode === 'pagination') return

  state.currentPage++
  const photos = await fetchPhotos(state.currentQuery, state.currentPage)
  state.photos = [...state.photos, ...photos]
  displayPhotos(photos, true)
}
```

Кнопка "Завантажити більше":

```javascript
export function setupLoadMoreButton() {
  elements.loadMoreBtn.addEventListener('click', async () => {
    await loadMorePhotos()
  })
}
```

Infinite scroll з debouncing:

```javascript
function isBottomReached() {
  const scrollTop = window.pageYOffset || document.documentElement.scrollTop
  const scrollHeight = document.documentElement.scrollHeight
  const clientHeight = document.documentElement.clientHeight

  return scrollTop + clientHeight >= scrollHeight - 300
}

let scrollTimeout

function handleScroll() {
  clearTimeout(scrollTimeout)

  scrollTimeout = setTimeout(async () => {
    if (isBottomReached() &&
        state.currentTab === 'all' &&
        !state.isLoading &&
        state.loadingMode === 'infinite') {
      await loadMorePhotos()
    }
  }, 200)
}

export function setupInfiniteScroll() {
  window.addEventListener('scroll', handleScroll)
}
```

Пагінація з номерами сторінок:

```javascript
export function renderPagination(currentPage, totalPages) {
  elements.paginationNav.innerHTML = ''

  const maxVisiblePages = 5
  let startPage = Math.max(1, currentPage - 2)
  let endPage = Math.min(totalPages, currentPage + 2)

  // Кнопка "Попередня"
  const prevLi = document.createElement('li')
  prevLi.className = `page-item ${currentPage === 1 ? 'disabled' : ''}`
  prevLi.innerHTML = `
    <button class="page-link" data-page="${currentPage - 1}">
      Попередня
    </button>
  `
  elements.paginationNav.appendChild(prevLi)

  // Номери сторінок
  for (let i = startPage; i <= endPage; i++) {
    const li = document.createElement('li')
    li.className = `page-item ${i === currentPage ? 'active' : ''}`
    li.innerHTML = `<button class="page-link" data-page="${i}">${i}</button>`
    elements.paginationNav.appendChild(li)
  }

  // Кнопка "Наступна"
  const nextLi = document.createElement('li')
  nextLi.className = `page-item ${currentPage === totalPages ? 'disabled' : ''}`
  nextLi.innerHTML = `
    <button class="page-link" data-page="${currentPage + 1}">
      Наступна
    </button>
  `
  elements.paginationNav.appendChild(nextLi)
}
```

Обробка кліків на пагінації:

```javascript
export function setupPaginationClick() {
  elements.paginationNav.addEventListener('click', async (e) => {
    if (e.target.classList.contains('page-link')) {
      const page = parseInt(e.target.dataset.page)
      if (page && !isNaN(page)) {
        state.currentPage = page
        const photos = await fetchPhotos(state.currentQuery, page)
        state.photos = photos
        displayPhotos(photos)
        window.scrollTo({ top: 0, behavior: 'smooth' })
      }
    }
  })
}
```

**[СКРІН 14: Пагінація з номерами сторінок]**

**[СКРІН 15: Infinite scroll з автозавантаженням]**

\newpage

## 3.10 Використання localStorage

### Завдання

Зберігати дані у браузері через localStorage, використовувати JSON.stringify/parse для перетворення об'єктів, реалізувати читання, запис та видалення даних.

### Рішення для Photo Gallery

В Photo Gallery реалізовано систему улюблених фотографій через localStorage. Створено функції getFavorites() та saveFavorites() для роботи зі сховищем. Масив JavaScript об'єктів перетворюється у JSON рядок через JSON.stringify() при збереженні та парситься через JSON.parse() при читанні.

Функція toggleFavorite() використовує findIndex() для перевірки наявності фото та splice()/push() для видалення/додавання. Всі операції обгорнуто в try-catch для обробки помилок. Додано фільтрацію улюблених за мінімальною кількістю лайків.

### Код рішення

Збереження даних (src/storage.js):

```javascript
export function saveFavorites(favorites) {
  try {
    localStorage.setItem('favorites', JSON.stringify(favorites))
    updateFavoritesCount()
  } catch (error) {
    console.error('Помилка запису в localStorage:', error)
    showError('Не вдалося зберегти улюблене фото')
  }
}
```

Читання даних:

```javascript
export function getFavorites() {
  try {
    const favorites = localStorage.getItem('favorites')
    return favorites ? JSON.parse(favorites) : []
  } catch (error) {
    console.error('Помилка читання з localStorage:', error)
    return []
  }
}
```

Перевірка наявності:

```javascript
export function isFavorite(photoId) {
  const favorites = getFavorites()
  return favorites.some(fav => fav.id === photoId)
}
```

Додавання/видалення:

```javascript
export function toggleFavorite(photo) {
  const favorites = getFavorites()
  const index = favorites.findIndex(fav => fav.id === photo.id)

  if (index > -1) {
    // Видалити з улюблених
    favorites.splice(index, 1)
  } else {
    // Додати до улюблених
    favorites.push(photo)
  }

  saveFavorites(favorites)

  // Оновити відображення якщо на вкладці "Улюблені"
  if (state.currentTab === 'favorites') {
    displayPhotos(favorites)
  }
}
```

Оновлення лічильника:

```javascript
export function updateFavoritesCount() {
  const favorites = getFavorites()
  elements.favoritesCount.textContent = favorites.length
}
```

Фільтрація улюблених:

```javascript
elements.favoritesTab.addEventListener('click', () => {
  state.currentTab = 'favorites'
  elements.favoritesTab.classList.add('active')
  elements.allTab.classList.remove('active')

  // Показати поле фільтра
  elements.minLikesContainer.classList.remove('d-none')

  let favorites = getFavorites()

  // Застосувати фільтр по лайках
  if (state.minLikes > 0) {
    favorites = favorites.filter(photo => photo.likes >= state.minLikes)
  }

  displayPhotos(favorites)
})
```

**[СКРІН 16: Вкладка "Улюблені" зі збереженими фото]**

**[СКРІН 17: localStorage в Chrome DevTools]**

\newpage

# 4. ФУНКЦІОНАЛЬНІ МОЖЛИВОСТІ

Photo Gallery - це повнофункціональний веб-застосунок для роботи з фотографіями з Unsplash API. Застосунок надає користувачу зручний інтерфейс для пошуку, перегляду та збереження улюблених фотографій.

## 4.1 Пошук фотографій за ключовими словами

Користувач може шукати фотографії за будь-якими ключовими словами англійською мовою. Форма пошуку має валідацію - не дозволяє відправити порожній запит. Після введення ключового слова та натискання кнопки "Пошук" або клавіші Enter, відправляється запит до Unsplash API.

Під час завантаження показується індикатор (spinner) з текстом "Завантаження...". Результати відображаються у вигляді адаптивної галереї карток - 3 колонки на desktop, 2 на tablet, 1 на mobile. Кожна картка містить зображення, ім'я автора, кількість вподобань та кнопку додавання до улюблених.

Приклади популярних запитів: nature, ocean, city, mountain, sunset, architecture, cat, technology.

## 4.2 Категорії для швидкого доступу

Горизонтальна навігація з кнопками категорій дозволяє швидко переключатися між популярними темами без введення тексту: Featured, Wallpapers, 3D Renders, Nature, Textures, Film, Architecture, Street Photography, Experimental, Travel, People.

Категорії можна прокручувати горизонтально (на мобільних - свайпом). При кліку на категорію вона стає активною (темний фон, білий текст) і автоматично виконується пошук по цій темі. Лічильник сторінок скидається на 1.

## 4.3 Сортування результатів

Випадаючий список "Сортування за популярністю" надає три варіанти:

- **Релевантність (за замовчуванням)** - Unsplash API сам визначає найкращі відповідності запиту
- **Від найпопулярніших ⬇️** - спочатку фото з найбільшою кількістю лайків (тисячі вподобань)
- **Від найновіших 🕒** - найсвіжіші фотографії, недавно завантажені на Unsplash

При зміні сортування галерея автоматично перезавантажується з новим порядком. Сортування застосовується до всіх наступних завантажень фото.

## 4.4 Три режими завантаження

Застосунок пропонує три режими завантаження додаткових фотографій на вибір користувача:

### Пагінація (📄)

Традиційна навігація з номерами сторінок та кнопками "Попередня"/"Наступна". Показується максимум 5 видимих номерів з поточною сторінкою в центрі. Віддалені сторінки замінені трикрапкою (...). При кліку на номер фотографії повністю замінюються новими і виконується плавна прокрутка до верху сторінки.

**Переваги:** чіткий контроль, легко повернутись на конкретну сторінку.

### Завантажити більше (➕)

Кнопка внизу галереї яка при натисканні завантажує наступні 12 фотографій та додає їх до існуючих (не замінює). Галерея поступово збільшується. Можна завантажувати необмежену кількість разів. Коли фото закінчуються - кнопка ховається і показується повідомлення "📷 Це всі фото за вашим запитом".

**Переваги:** контроль завантаження, економія трафіку, не втрачаються попередні фото.

### Infinite Scroll (∞)

Автоматичне завантаження нових фотографій при прокручуванні. За 300 пікселів до кінця сторінки автоматично запускається завантаження наступних 12 фото. Показується spinner, нові фото додаються знизу. Можна скролити безперервно. Використано debouncing (максимум раз на 200мс) для запобігання надмірним запитам.

**Переваги:** найкращий UX, безперервний перегляд, не потрібно нічого натискати.

## 4.5 Перегляд фотографій у повному розмірі

При кліку на будь-яку картку фотографії (крім кнопки улюбленого) відкривається модальне вікно Bootstrap на повний екран. У вікні відображається:

- Велике зображення високої якості (regular size, ~1080px ширина)
- Заголовок з описом фотографії (якщо є)
- Опис під зображенням
- Інформація про автора: Ім'я (@username)
- Кількість вподобань

Внизу дві кнопки:
- **Закрити** (сіра) - закриває модальне вікно
- **Відкрити оригінал** (синя) - відкриває сторінку фото на Unsplash.com у новій вкладці

Закрити вікно можна також клавішею ESC, кліком поза вікном або на ✕ у правому верхньому куті.

## 4.6 Система улюблених фотографій

### Додавання до улюблених

Кнопка з білим серцем 🤍 у правому верхньому куті кожної картки. При кліку серце стає червоним ❤️, фон кнопки змінюється на червоний, лічильник у вкладці "Улюблені" збільшується. Дані автоматично зберігаються в localStorage браузера.

### Вкладка "Улюблені"

Клік на вкладку "❤️ Улюблені (N)" показує тільки збережені фото. Вкладка стає активною (жирний текст, підкреслена). З'являється поле "Мінімум лайків (тільки для улюблених)" для фільтрації.

### Фільтрація улюблених за лайками

На вкладці "Улюблені" доступне поле для введення мінімальної кількості лайків. Наприклад, введення "100" показує тільки ті улюблені фото що мають 100+ вподобань. Фільтр працює з debouncing (500мс затримка) для оптимізації.

### Видалення з улюблених

Клік на червоне серце ❤️ робить його білим 🤍 знову. Лічильник зменшується. Якщо це було на вкладці "Улюблені" - картка зникає з галереї.

### Збереження даних

Всі улюблені зберігаються в localStorage браузера. Дані зберігаються навіть після закриття вкладки, браузера або перезавантаження комп'ютера. Дані НЕ синхронізуються між пристроями (локально для кожного браузера). Очищення даних сайту в налаштуваннях браузера видалить улюблені.

## 4.7 Адаптивний дизайн

Застосунок коректно відображається на всіх типах пристроїв:

**Desktop (великі екрани):** галерея з 3 колонками фотографій, всі елементи розташовані горизонтально, категорії прокручуються мишкою, hover ефекти на картках (підняття, збільшена тінь).

**Tablet (середні екрани):** галерея з 2 колонками, форма пошуку та фільтри в два ряди, категорії свайпаються пальцем.

**Mobile (малі екрани):** галерея з 1 колонкою, форма пошуку на всю ширину, кнопка пошуку під полем введення, вкладки та перемикачі режимів вертикально, модальне вікно на весь екран, оптимізовані розміри кнопок для пальців.

## 4.8 Обробка помилок та граничних випадків

### Помилки валідації

Порожній запит пошуку: показується червоний alert "Будь ласка, введіть пошуковий запит". Alert автоматично ховається при наступному запиті.

### Помилки API

- **401 Unauthorized:** "Невірний API ключ. Будь ласка, налаштуйте UNSPLASH_ACCESS_KEY в config.js"
- **403 Forbidden:** "Перевищено ліміт запитів до API. Спробуйте пізніше." (Unsplash має ліміт 50 запитів на годину для демо ключів)
- **Інші помилки сервера:** "Помилка API: 404" (або інший код статусу)

### Помилки мережі

"Помилка мережі. Перевірте з'єднання з інтернетом." - виникає коли немає інтернету або заблоковано запити.

### Помилки localStorage

"Не вдалося зберегти улюблене фото" - у приватному режимі деяких браузерів localStorage може бути недоступний.

### Граничні випадки

**Відсутні результати пошуку:** великий Empty state з іконкою камери 📷, текст "Нічого не знайдено" та "Спробуйте інший пошуковий запит".

**Кінець результатів:** у режимах "Завантажити більше" та "Infinite Scroll" показується повідомлення "📷 Це всі фото за вашим запитом". Попередні фото залишаються на екрані, кнопки завантаження ховаються.

**Порожні улюблені:** Empty state на вкладці "Улюблені" з підказкою додати фото до улюблених. Лічильник показує (0).

\newpage

# 5. СТРУКТУРА ПРОЄКТУ

## 5.1 Модульна архітектура

Проєкт організовано за принципом розділення відповідальностей (Separation of Concerns) з використанням ES6 модулів:

```
photo-gallery/
├── public/
│   └── vite.svg                 # Іконка Vite
├── src/
│   ├── config.js                # Конфігурація API
│   ├── state.js                 # Глобальний стан застосунку
│   ├── dom.js                   # Посилання на DOM елементи
│   ├── storage.js               # Робота з localStorage
│   ├── api.js                   # HTTP запити до Unsplash API
│   ├── ui.js                    # Функції відображення UI
│   ├── events.js                # Обробники подій
│   ├── main.js                  # Точка входу, ініціалізація
│   └── style.css                # Власні стилі
├── .git/                        # Git репозиторій
├── .gitignore                   # Виключення для Git
├── index.html                   # HTML структура
├── package.json                 # Залежності та скрипти
└── README.md                    # Документація проєкту
```

## 5.2 Опис модулів

**config.js** - містить константи конфігурації: UNSPLASH_ACCESS_KEY (ключ доступу до API), UNSPLASH_API_URL (базовий URL API).

**state.js** - глобальний стан застосунку: currentPage, currentQuery, photos, currentTab, isLoading, loadingMode, totalPages, sortOrder, minLikes.

**dom.js** - централізовані посилання на всі DOM елементи: форма пошуку, галерея, модальне вікно, вкладки, кнопки, фільтри.

**storage.js** - функції роботи з localStorage: getFavorites(), saveFavorites(), isFavorite(), toggleFavorite(), updateFavoritesCount().

**api.js** - функції для роботи з Unsplash API: fetchPhotos() з обробкою помилок та управлінням станом завантаження.

**ui.js** - функції відображення UI: createPhotoCard(), displayPhotos(), showPhotoModal(), renderPagination(), showLoading(), showError().

**events.js** - обробники подій: setupSearchForm(), setupGalleryClick(), setupInfiniteScroll(), setupTabs(), setupCategories(), setupLoadingModeToggle(), setupSortFilter(), setupLikesFilter(), setupEventHandlers().

**main.js** - точка входу застосунку: імпорт всіх модулів, функція init() для ініціалізації, завантаження початкових фотографій.

**style.css** - власні стилі: reset, стилізація карток, кнопка улюбленого, анімації (fadeIn, bounce), категорії, фільтри, адаптивність (@media queries).

## 5.3 Залежності проєкту

**Production залежності:**
- **axios** (1.13.2) - HTTP клієнт для запитів до API
- **bootstrap** (5.3.8) - UI фреймворк для стилізації

**Dev залежності:**
- **vite** (7.2.4) - швидкий збирач та dev-сервер з HMR

## 5.4 Git конфігурація

Файл .gitignore виключає: node_modules, dist, dist-ssr, *.local, системні файли .DS_Store.

\newpage

# 6. ІНСТРУКЦІЯ З ВИКОРИСТАННЯ

## 6.1 Встановлення проєкту

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/AlexBram003/Practika.git
cd Practika
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

Буде встановлено: vite@7.2.4, axios@1.13.2, bootstrap@5.3.8

### Крок 3: Налаштування API ключа

1. Перейти на https://unsplash.com/developers
2. Зареєструватись або увійти в акаунт
3. Натиснути "New Application"
4. Прийняти умови використання
5. Заповнити форму заявки (Application name: Photo Gallery)
6. Скопіювати "Access Key"
7. Відкрити файл `src/config.js`
8. Замінити значення: `export const UNSPLASH_ACCESS_KEY = 'ваш_ключ_тут'`

### Крок 4: Запуск development сервера

```bash
npm run dev
```

Відкрити браузер та перейти на http://localhost:5173

## 6.2 Використання застосунку

**Пошук фотографій:** ввести ключове слово англійською (nature, city, cat), натиснути "Пошук" або Enter.

**Вибір категорії:** прокрутити горизонтальні категорії, клікнути на цікаву (Architecture).

**Зміна сортування:** відкрити список "Сортування за популярністю", вибрати потрібний варіант.

**Вибір режиму завантаження:** клікнути один з трьох перемикачів (Пагінація / Завантажити більше / Infinite Scroll).

**Додавання до улюблених:** навести на фото, клікнути біле серце 🤍.

**Перегляд у повному розмірі:** клікнути на будь-яке фото, переглянути деталі.

**Перегляд улюблених:** клікнути вкладку "❤️ Улюблені", ввести мінімум лайків для фільтрації.

## 6.3 Збірка для production

```bash
npm run build
```

Оптимізована збірка буде в папці `dist/`

Попередній перегляд: `npm run preview`

Deployment: папку `dist/` можна розмістити на GitHub Pages, Netlify, Vercel, Firebase Hosting.

\newpage

# 7. ВИСНОВКИ

В результаті виконання навчальної практики з розробки WEB-застосунків було створено повнофункціональний веб-застосунок **Photo Gallery** з використанням сучасних технологій та інструментів.

## Досягнуті результати

**Всі 10 тем практики повністю реалізовані:**

1. ✅ **Створення та налаштування WEB-проєкту** - Vite, Git, модульна структура
2. ✅ **Робота з DOM-деревом** - getElementById, createElement, appendChild, classList
3. ✅ **Обробка подій користувача** - submit, click, scroll, делегування, debouncing
4. ✅ **Робота з масивами та об'єктами** - forEach, find, some, filter, spread operator
5. ✅ **Розробка HTML-форм та валідація** - required, JavaScript валідація, trim()
6. ✅ **Використання Bootstrap та Axios** - Navbar, Modal, Tabs, Grid, HTTP запити
7. ✅ **Взаємодія з зовнішніми API** - Unsplash API, JSON, async/await
8. ✅ **Обробка помилок у JavaScript** - try-catch, типи помилок, повідомлення
9. ✅ **Пагінація та нескінченний скрол** - три режими завантаження
10. ✅ **Використання localStorage** - JSON.stringify/parse, збереження улюблених

**Додаткові досягнення:**

- Горизонтальна навігація з 11 категоріями
- Система сортування (релевантність, популярність, новизна)
- Три режими завантаження з перемикачем
- Фільтрація улюблених за мінімальною кількістю лайків
- Інтелектуальна пагінація з трикрапкою
- Коректна обробка кінця результатів
- Модульна архітектура (8 файлів)
- Використання Bootstrap класів замість inline стилів
- Адаптивний дизайн (desktop/tablet/mobile)
- Плавні анімації (fadeIn, bounce, smooth scroll)

## Набуті навички

**Технічні:** Vite, Axios, Bootstrap 5, Unsplash API, Git, Chrome DevTools, ES6+ (модулі, async/await, деструктуризація, spread).

**Методологічні:** модульна архітектура (SRP), асинхронне програмування, обробка помилок, оптимізація (debouncing), event delegation, immutable підхід, документування коду.

**UX/UI:** інтуїтивний інтерфейс, вибір режимів, візуальний feedback, адаптивний дизайн, accessibility.

## Практична цінність

Застосунок демонструє реальну інтеграцію з зовнішнім API, сучасні підходи до веб-розробки, best practices JavaScript, продуманий UX та готовність до deployment.

Проєкт повністю відповідає методичним рекомендаціям та охоплює всі обов'язкові теми з детальними прикладами коду та демонстрацією функціональності.

\newpage

# 8. СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ

## Офіційна документація

1. **Unsplash API Documentation**
   https://unsplash.com/documentation

2. **Vite Documentation**
   https://vitejs.dev/guide/

3. **Bootstrap 5 Documentation**
   https://getbootstrap.com/docs/5.3/

4. **Axios Documentation**
   https://axios-http.com/docs/intro

5. **MDN Web Docs - JavaScript**
   https://developer.mozilla.org/en-US/docs/Web/JavaScript

6. **MDN Web Docs - HTML**
   https://developer.mozilla.org/en-US/docs/Web/HTML

7. **MDN Web Docs - CSS**
   https://developer.mozilla.org/en-US/docs/Web/CSS

8. **MDN Web Docs - Web Storage API**
   https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API

## Навчальні матеріали

9. **JavaScript.info** - Сучасний підручник JavaScript
   https://uk.javascript.info/

10. **W3Schools - JavaScript Tutorial**
    https://www.w3schools.com/js/

11. **Eloquent JavaScript** (4th Edition) - Marijn Haverbeke
    https://eloquentjavascript.net/

## Методичні рекомендації

12. **Навчальна практика з розробки WEB-застосунків**
    Методичні рекомендації щодо виконання практичних завдань
    Олександрійський політехнічний фаховий коледж, 2025

## Інструменти розробки

13. **Git Documentation**
    https://git-scm.com/doc

14. **NPM Documentation**
    https://docs.npmjs.com/

15. **Chrome DevTools**
    https://developer.chrome.com/docs/devtools/

16. **Visual Studio Code**
    https://code.visualstudio.com/docs
