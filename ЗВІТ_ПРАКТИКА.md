---
title: "ЗВІТ З НАВЧАЛЬНОЇ ПРАКТИКИ"
geometry: "left=2cm,right=1cm,top=2cm,bottom=2cm"
fontsize: 14pt
mainfont: "Times New Roman"
documentclass: article
linestretch: 1.5
indent: true
---

\begin{titlepage}
\centering

{\Large ОЛЕКСАНДРІЙСЬКИЙ ПОЛІТЕХНІЧНИЙ ФАХОВИЙ КОЛЕДЖ}

\vspace{1cm}

Циклова комісія комп'ютерних дисциплін

\vspace{3cm}

{\Large\textbf{ЗВІТ}}

\vspace{0.5cm}

з навчальної практики

\vspace{0.5cm}

з розробки WEB-застосунків

\vspace{0.5cm}

НП 121.242.000

\vspace{2cm}

\begin{flushleft}
Студента ІV курсу ОП-221 групи

Освітньо-професійної програми

«Інженерія програмного забезпечення»

Олександра БРАМА

\vspace{1cm}

Керівники викладачі спецдисциплін,

Едуард БОЙКО

Андрій КОРНІЄНКО

\vspace{1cm}

Оцінка \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
\end{flushleft}

\vfill

м. Олександрія

2025 рік

\end{titlepage}

\newpage

# ЗМІСТ

1. ВСТУП ........................................ 3
2. ОПИС ПРОЄКТУ ................................ 4
3. РЕАЛІЗОВАНІ ТЕМИ ПРАКТИКИ ................... 6
   - 3.1 Створення та налаштування WEB-проєкту .. 6
   - 3.2 Робота з DOM-деревом ................... 7
   - 3.3 Обробка подій користувача .............. 8
   - 3.4 Робота з масивами та об'єктами ......... 9
   - 3.5 Розробка HTML-форм та валідація ........ 10
   - 3.6 Використання Bootstrap та Axios ........ 11
   - 3.7 Взаємодія з зовнішніми API ............. 12
   - 3.8 Обробка помилок у JavaScript ........... 13
   - 3.9 Пагінація та нескінченний скрол ....... 14
   - 3.10 Використання localStorage ............. 15
4. ФУНКЦІОНАЛЬНІ МОЖЛИВОСТІ .................... 16
5. СТРУКТУРА ПРОЄКТУ ........................... 26
6. ІНСТРУКЦІЯ З ВИКОРИСТАННЯ ................... 28
7. ВИСНОВКИ .................................... 30
8. СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ .................. 31

\newpage

# 1. ВСТУП

Метою навчальної практики є закріплення та поглиблення знань, отриманих під час теоретичного навчання, застосування необхідних вмінь та навичок у розробці веб-застосунків.

В рамках практики було обрано **Варіант 6: Photo Gallery** – веб-застосунок для відображення добірки фотографій з Unsplash API із можливістю перегляду в повному розмірі.

## Завдання практики

Основними завданнями навчальної практики є:

- Оволодіння сучасними методами та інструментами розробки вебзастосунків, зокрема роботою з HTML, CSS, JavaScript та системами контролю версій.

- Закріплення та розвиток знань і вмінь, отриманих студентами під час теоретичного навчання, шляхом виконання практичного проєкту.

- Набуття навичок проєктування, створення та тестування клієнтських вебзастосунків.

- Виховання відповідальності за результати своєї роботи та вміння працювати самостійно, використовуючи системи керування проєктами.

- Формування цифрової культури праці, дбайливого ставлення до ресурсів та дотримання правил кібербезпеки.

## Обраний варіант

**Варіант 6: Photo Gallery** – відображати добірку фотографій з API (Unsplash) із можливістю перегляду в повному розмірі.

\newpage

# 2. ОПИС ПРОЄКТУ

**Photo Gallery** – це інтерактивний веб-застосунок для пошуку та перегляду фотографій з безкоштовного API Unsplash.

## Основні характеристики

- **Тип застосунку:** Single Page Application (SPA)
- **API:** Unsplash API
- **Збирач модулів:** Vite 7.2.4
- **Фреймворк UI:** Bootstrap 5.3.8
- **HTTP клієнт:** Axios 1.13.2
- **Мова програмування:** JavaScript ES6+
- **Система контролю версій:** Git

## Функціональність застосунку

1. **Пошук фотографій** за ключовими словами через форму пошуку.

2. **Відображення результатів** у вигляді адаптивної галереї з картками фотографій.

3. **Категорії** – швидкий доступ до популярних тем (Nature, Architecture, Travel тощо).

4. **Сортування** – можливість сортувати фото за популярністю, релевантністю або датою.

5. **Три режими завантаження:**
   - Пагінація з номерами сторінок
   - Кнопка "Завантажити більше"
   - Нескінченний скрол (Infinite Scroll)

6. **Перегляд у повному розмірі** – модальне вікно з детальною інформацією про фото.

7. **Збереження улюблених** – можливість додавати фото в улюблені з використанням localStorage та фільтрувати їх за мінімальною кількістю лайків.

8. **Обробка помилок** – детальні повідомлення про помилки API, мережі та валідації.

**[СКРІН 1: Головна сторінка Photo Gallery]**

*(Має бути зображення головної сторінки з:*
*- навігаційною панеллю "📸 Photo Gallery"*
*- формою пошуку з полем введення та кнопкою*
*- горизонтальною навігацією категорій*
*- фільтрами сортування*
*- вкладками "Всі фото" та "Улюблені"*
*- перемикачами режимів завантаження*
*- галереєю фотографій природи (3 колонки)*
*- підказкою "Прокручуйте вниз для автоматичного завантаження")*

\newpage

# 3. РЕАЛІЗОВАНІ ТЕМИ ПРАКТИКИ

## 3.1 Створення та налаштування WEB-проєкту

У рамках цієї теми було виконано повну ініціалізацію сучасного веб-проєкту з використанням інструменту збірки Vite. Проєкт створено за допомогою команди `npm create vite@latest` з шаблоном vanilla JavaScript, що забезпечує швидку розробку без зайвих залежностей від фреймворків.

Налаштовано файл `package.json` з необхідними залежностями, включаючи Bootstrap 5.3.8 для стилізації інтерфейсу та Axios 1.13.2 для роботи з HTTP запитами до зовнішнього API. Створено скрипти для запуску dev-сервера (`npm run dev`), збірки проєкту (`npm run build`) та попереднього перегляду production версії (`npm run preview`).

Ініціалізовано Git репозиторій для контролю версій коду, налаштовано файл `.gitignore` для виключення папок `node_modules`, `dist` та інших тимчасових файлів. Створено базову HTML структуру з семантичними тегами, підключено головний JavaScript модуль через `<script type="module">` для підтримки ES6 модулів.

**Міні-висновок:** Успішно налаштовано професійне робоче середовище для розробки сучасного веб-застосунку. Використання Vite забезпечує швидку розробку завдяки Hot Module Replacement (HMR), а Git дозволяє відстежувати всі зміни в коді. Проєкт готовий до розширення та масштабування.

\newpage

## 3.2 Робота з DOM-деревом

Реалізовано повноцінну систему роботи з Document Object Model для динамічного створення та управління елементами галереї фотографій. Використано різні методи вибору елементів: `getElementById()` для отримання посилань на унікальні елементи форми, галереї та модального вікна, а також `querySelectorAll()` для вибору колекції кнопок категорій.

Створено модульну систему посилань на DOM елементи через об'єкт `elements` у файлі `dom.js`, що забезпечує централізований доступ до всіх важливих елементів інтерфейсу. Це покращує читабельність коду та спрощує подальшу підтримку.

Реалізовано функцію динамічного створення карток фотографій за допомогою `document.createElement()` та `innerHTML`. Кожна картка містить зображення, інформацію про автора, кількість вподобань та кнопку для додавання до улюблених. Елементи додаються до DOM через метод `appendChild()`, що забезпечує ефективне відображення великої кількості фотографій.

Активно використовується управління класами через `classList.add()`, `classList.remove()` та `classList.toggle()` для показу/приховування елементів (спінер завантаження, повідомлення про помилки, кнопки завантаження) та зміни стану елементів (активна вкладка, улюблене фото). Замість inline стилів застосовуються Bootstrap класи (`d-none`, `active`), що забезпечує чистоту коду.

**Міні-висновок:** Ефективно реалізовано роботу з DOM без використання важких фреймворків. Модульний підхід до посилань на елементи та динамічне створення контенту забезпечують гнучкість та продуктивність застосунку. Використання Bootstrap класів замість inline стилів робить код більш підтримуваним.

\newpage

## 3.3 Обробка подій користувача

Створено комплексну систему обробки подій для забезпечення інтерактивності застосунку. Реалізовано обробник події `submit` для форми пошуку з використанням `preventDefault()` для запобігання перезавантаження сторінки. Форма виконує валідацію введених даних та ініціює пошук фотографій через API.

Застосовано паттерн делегування подій (event delegation) для обробки кліків на галереї. Замість додавання окремого обробника на кожну картку, один обробник на контейнері галереї перевіряє клік через `e.target.closest()`. Це оптимізує продуктивність, особливо при роботі з великою кількістю елементів, та автоматично працює з динамічно доданими картками.

Реалізовано обробку події `scroll` для нескінченного завантаження фотографій. Використано техніку debouncing з `setTimeout()` для обмеження частоти виконання функції до одного разу на 200мс, що запобігає надмірним запитам до API при швидкому скролінгу. Функція перевіряє позицію прокрутки та автоматично завантажує нові фотографії за 300 пікселів до кінця сторінки.

Додано обробники подій для перемикання між вкладками ("Всі фото" та "Улюблені"), зміни режимів завантаження (пагінація, завантажити більше, infinite scroll), вибору категорій та зміни сортування. Також реалізовано обробку кліків на номери сторінок у пагінації з делегуванням подій.

**Міні-висновок:** Комплексна система обробки подій забезпечує плавну та відгукову взаємодію користувача з застосунком. Використання делегування подій та debouncing оптимізує продуктивність навіть при роботі з сотнями елементів. Всі дії користувача обробляються миттєво з правильною валідацією та відповідними відповідями інтерфейсу.

\newpage

## 3.4 Робота з масивами та об'єктами

Активно використовуються сучасні методи роботи з масивами ES6+ для обробки колекцій фотографій та управління даними. Метод `forEach()` застосовується для ітерації по масиву фотографій та створення DOM елементів для кожного фото. Метод `find()` використовується для пошуку конкретної фотографії за унікальним ID при відкритті модального вікна або перевірці улюблених.

Метод `some()` ефективно перевіряє наявність фотографії в масиві улюблених, повертаючи `true` або `false` без необхідності перебору всього масиву. Комбінація методів `findIndex()` та `splice()` використовується для видалення фотографій з улюблених - спочатку знаходиться індекс елемента, потім він видаляється з масиву.

Spread оператор (`...`) застосовується для об'єднання масивів при додаванні нових фотографій до існуючих у режимі нескінченного скролу. Замість мутації оригінального масиву створюється новий масив, що відповідає принципам функціонального програмування та спрощує відстеження змін стану.

Метод `filter()` використовується для фільтрації улюблених фотографій за мінімальною кількістю лайків, створюючи новий масив з елементів, що відповідають умові. Деструктуризація об'єктів застосовується при роботі з даними від API для зручного доступу до вкладених властивостей фотографій (urls, user, likes).

**Міні-висновок:** Ефективне використання методів масивів ES6+ дозволяє писати чистий, виразний та функціональний код. Уникання прямої мутації даних через spread оператор та immutable методи робить код більш передбачуваним та легшим для налагодження. Всі операції з даними виконуються оптимально завдяки вбудованим методам JavaScript.

\newpage

## 3.5 Розробка HTML-форм та валідація

Створено повноцінну форму пошуку з використанням семантичних HTML елементів та багаторівневою системою валідації. HTML форма містить текстове поле `<input type="text">` з атрибутом `required` для базової браузерної валідації та placeholder з підказками для користувача. Кнопка submit виконує відправку форми як натисканням миші, так і клавішею Enter.

Реалізовано додаткову JavaScript валідацію, яка перевіряє чи не є поле порожнім після очищення пробілів методом `trim()`. Це запобігає відправці форми з лише пробілами, що не блокується стандартною HTML валідацією. При невалідних даних користувачу показується зрозуміле повідомлення про помилку через Bootstrap alert компонент.

Додано форму фільтрації з `<select>` для вибору типу сортування (релевантність, популярні, найновіші) та `<input type="number">` для введення мінімальної кількості лайків. Числове поле має атрибути `min="0"` та `step="10"` для зручності користувача та запобігання введенню некоректних значень.

Значення з форм зчитуються через властивість `.value` з подальшою обробкою та використанням для запитів до API. Форма фільтрації використовує debouncing для оптимізації - зміни застосовуються лише через 500мс після останнього введення, що запобігає надмірним запитам при швидкому наборі.

**Міні-висновок:** Комбінація HTML5 валідації та JavaScript перевірок забезпечує надійний контроль введених даних на всіх рівнях. Використання правильних типів input та атрибутів покращує користувацький досвід, особливо на мобільних пристроях де з'являється відповідна клавіатура. Чіткі повідомлення про помилки допомагають користувачам швидко виправити проблеми.

\newpage

## 3.6 Використання Bootstrap та Axios

Інтегровано Bootstrap 5.3.8 для створення сучасного, адаптивного інтерфейсу без необхідності писати великі об'єми власних CSS стилів. Використано компонент Navbar для створення темної навігаційної панелі з логотипом застосунку. Card компоненти застосовуються для візуального групування форми пошуку та фільтрів.

Реалізовано Modal (модальне вікно) для відображення фотографій у повному розмірі з деталями. Модальне вікно ініціалізується через JavaScript API Bootstrap з використанням методу `new bootstrap.Modal()`, а показ та приховування виконується через методи `.show()` та `.hide()`. Tabs компонент використовується для перемикання між вкладками "Всі фото" та "Улюблені" з індикацією кількості улюблених.

Grid система Bootstrap забезпечує адаптивне розташування карток фотографій - 3 колонки на великих екранах, 2 на середніх та 1 на мобільних пристроях. Utility класи (`d-none`, `mb-3`, `text-center`) використовуються для швидкого стилізування без написання власного CSS. Form controls забезпечують красиві та функціональні поля введення.

Axios інтегровано для виконання HTTP запитів до Unsplash API. Бібліотека автоматично перетворює JSON відповіді в JavaScript об'єкти, обробляє помилки через promise rejection та дозволяє передавати параметри запиту через об'єкт `params`. Це спрощує код порівняно з використанням нативного `fetch()` API.

Налаштовано конфігурацію запитів з параметрами `query`, `page`, `per_page`, `order_by` та `client_id`. Відповіді з API обробляються через async/await синтаксис, що робить асинхронний код читабельним як синхронний. Структура відповіді `response.data.results` містить масив об'єктів фотографій з усіма необхідними даними.

**Міні-висновок:** Використання Bootstrap значно прискорило розробку інтерфейсу та забезпечило професійний вигляд застосунку без глибоких знань CSS. Axios спростив роботу з API завдяки зручному API та автоматичній обробці JSON. Комбінація цих інструментів дозволила зосередитись на бізнес-логіці замість технічних деталей.

\newpage

## 3.7 Взаємодія з зовнішніми API

Реалізовано повноцінну інтеграцію з Unsplash API - безкоштовним сервісом для отримання високоякісних фотографій. Налаштовано автентифікацію через Access Key, який передається в кожному запиті через параметр `client_id`. API надає можливість пошуку фотографій за ключовими словами, пагінації результатів та сортування за різними критеріями.

Створено асинхронну функцію `fetchPhotos()` яка використовує Axios для відправки GET запитів до endpoint `/search/photos`. Функція приймає параметри пошукового запиту та номер сторінки, формує об'єкт параметрів і відправляє запит. Додано параметр `per_page` для контролю кількості фотографій на сторінку та `order_by` для сортування результатів.

Обробка JSON відповідей виконується автоматично через Axios. Структура даних від API включає масив `results` з об'єктами фотографій, кожен з яких містить:
- `id` - унікальний ідентифікатор
- `urls` - об'єкт з посиланнями на різні розміри зображень (small, regular, full)
- `user` - об'єкт з даними автора (name, username, profile_image)
- `likes` - кількість вподобань
- `description` та `alt_description` - описи фотографії

Додано індикатор завантаження (spinner) який показується на час виконання запиту через зміну стану `isLoading` та виклик функції `showLoading()`. Це покращує UX, даючи користувачу зрозуміти що відбувається обробка запиту. Після отримання даних spinner автоматично ховається.

Реалізовано роботу з параметрами запиту включаючи динамічну зміну query (пошуковий запит), page (номер сторінки), order_by (сортування: latest, popular, relevant). Параметри формуються динамічно залежно від дій користувача - вибору категорії, зміни сортування або введення в поле пошуку.

**Міні-висновок:** Успішна інтеграція з зовнішнім API демонструє розуміння принципів роботи RESTful сервісів. Правильна обробка асинхронних запитів, структурування даних та відображення індикаторів стану забезпечують професійний користувацький досвід. Unsplash API надає якісний контент, а застосунок ефективно його використовує.

\newpage

## 3.8 Обробка помилок у JavaScript

Створено багаторівневу систему обробки помилок для забезпечення стабільної роботи застосунку в різних сценаріях. Всі асинхронні операції обгорнуто в `try-catch` блоки для перехоплення помилок на ранній стадії. Це запобігає падінню застосунку та дозволяє показати користувачу зрозуміле повідомлення замість технічних помилок у консолі.

Реалізовано детальну обробку помилок від API з розрізненням типів:
- `error.response` - сервер відповів з помилкою (401 Unauthorized, 403 Forbidden тощо)
- `error.request` - запит відправлено але відповіді немає (проблеми з мережею)
- Інші помилки - проблеми в коді або конфігурації

Для кожного типу помилки створено специфічне повідомлення українською мовою, що допомагає користувачу зрозуміти проблему та можливі шляхи вирішення. Наприклад, при помилці 403 повідомлення пояснює що перевищено ліміт запитів та рекомендує спробувати пізніше.

Використовується async/await синтаксис замість promise chains для більш читабельного коду обробки помилок. Це дозволяє писати асинхронний код у стилі синхронного з чіткою структурою try-catch, що спрощує розуміння потоку виконання програми.

Додано обробку помилок localStorage через try-catch при читанні та записі даних. У деяких браузерах або режимах приватного перегляду localStorage може бути недоступний або переповнений. Помилки логуються в консоль через `console.error()` для налагодження, а користувачу показується зрозуміле повідомлення.

Всі помилки супроводжуються візуальним feedback - показується червоний Bootstrap alert з описом проблеми. Попередні помідомлення про помилки автоматично ховаються при новому запиті через функцію `hideError()`, що запобігає плутанині користувача старими повідомленнями.

**Міні-висновок:** Ретельна обробка помилок перетворює потенційно нестабільний застосунок у надійний продукт. Користувач завжди отримує зрозуміле пояснення що сталось та що робити далі. Логування помилок в консоль допомагає розробникам швидко знаходити та виправляти проблеми. Try-catch блоки гарантують що застосунок продовжить працювати навіть при несподіваних помилках.

\newpage

## 3.9 Пагінація та нескінченний скрол

Реалізовано три різні режими завантаження фотографій для надання користувачу гнучкості у виборі зручного способу перегляду контенту. Кожен режим має свої переваги та підходить для різних сценаріїв використання.

**Режим пагінації** відображає традиційну систему з номерами сторінок та кнопками "Попередня"/"Наступна". Реалізовано інтелектуальне відображення номерів сторінок - показується максимум 5 сторінок одночасно з поточною в центрі, а віддалені сторінки замінюються трикрапкою. При кліку на номер сторінки фотографії повністю замінюються новими з плавною прокруткою вгору.

**Режим "Завантажити більше"** додає кнопку внизу галереї, при натисканні на яку завантажуються додаткові 12 фотографій та додаються до існуючих. Цей режим зручний для користувачів які хочуть контролювати коли завантажувати нові дані, особливо на повільному інтернеті або обмеженому мобільному трафіку.

**Infinite Scroll (нескінченний скрол)** автоматично завантажує нові фотографії коли користувач прокручує майже до кінця сторінки. Реалізовано функцію `isBottomReached()` яка перевіряє позицію скролу та запускає завантаження за 300 пікселів до низу сторінки. Це забезпечує безперервний досвід перегляду без необхідності натискати кнопки.

Для оптимізації продуктивності застосовано техніку debouncing - функція обробки скролу виконується максимум раз на 200мс навіть якщо подія scroll спрацьовує частіше. Це запобігає сотням викликів функції при швидкому скролінгу та знижує навантаження на процесор. Використано `setTimeout()` та `clearTimeout()` для реалізації затримки.

Додано перевірку стану завантаження через прапорець `isLoading` щоб запобігти одночасним запитам. Якщо один запит ще виконується, новий не відправляється навіть якщо користувач досягнув кінця сторінки. Це економить ресурси сервера та запобігає дублюванню фотографій.

Реалізовано плавну прокрутку через CSS властивість `scroll-behavior: smooth`, що робить навігацію більш приємною для очей. При перемиканні сторінок у режимі пагінації виконується плавна прокрутка до верху через `window.scrollTo({ top: 0, behavior: 'smooth' })`.

**Міні-висновок:** Наявність трьох режимів завантаження робить застосунок універсальним для різних типів користувачів та сценаріїв використання. Infinite scroll забезпечує найкращий UX для швидкого перегляду великої кількості фото, пагінація дає чіткий контроль, а кнопка "Завантажити більше" економить трафік. Debouncing та перевірки стану гарантують оптимальну продуктивність навіть при інтенсивному використанні.

\newpage

## 3.10 Використання localStorage

Реалізовано повноцінну систему збереження улюблених фотографій у браузері користувача через Web Storage API. Дані зберігаються локально і залишаються доступними навіть після закриття браузера та перезавантаження комп'ютера, що створює персоналізований досвід використання застосунку.

Створено функції `getFavorites()` та `saveFavorites()` для роботи з localStorage. При збереженні масив JavaScript об'єктів перетворюється у JSON рядок через `JSON.stringify()`, оскільки localStorage може зберігати лише текстові дані. При читанні JSON рядок парситься назад у JavaScript об'єкт через `JSON.parse()`.

Функція `toggleFavorite()` реалізує логіку додавання та видалення фотографій з улюблених. Використовується метод `findIndex()` для перевірки чи фото вже в улюблених - якщо так, воно видаляється через `splice()`, якщо ні - додається через `push()`. Після кожної зміни оновлюється localStorage та лічильник улюблених у вкладці.

Всі операції з localStorage обгорнуто в try-catch блоки для обробки можливих помилок. У приватному режимі браузера або при переповненні сховища (зазвичай ліміт 5-10MB) може виникнути помилка. Замість падіння застосунку показується повідомлення користувачу та проблема логується в консоль для налагодження.

Реалізовано функцію `updateFavoritesCount()` яка оновлює лічильник у вкладці "Улюблені" кожного разу при зміні списку. Це дає користувачу візуальний feedback про кількість збережених фотографій. Лічильник автоматично оновлюється і при ініціалізації застосунку, читаючи дані з localStorage.

Додано фільтрацію улюблених фотографій за мінімальною кількістю лайків. Користувач може вказати порогове значення (наприклад 100) і бачити тільки ті улюблені фото що мають стільки або більше вподобань. Фільтр застосовується тільки до вкладки "Улюблені", не впливаючи на основний пошук.

**Міні-висновок:** localStorage забезпечує простий та ефективний спосіб збереження користувацьких даних без необхідності у backend сервері та базі даних. Функції JSON.stringify/parse дозволяють зберігати складні структури даних. Правильна обробка помилок гарантує що проблеми з сховищем не зламають весь застосунок. Система улюблених значно підвищує цінність застосунку, дозволяючи користувачам створювати персональні колекції.

\newpage

# 4. ФУНКЦІОНАЛЬНІ МОЖЛИВОСТІ

## 4.1 Початковий екран та інтерфейс

При завантаженні застосунку користувач бачить сучасний інтерфейс з чітко організованими елементами. Навігаційна панель містить логотип "📸 Photo Gallery". Під нею розташована форма пошуку з великим текстовим полем та синьою кнопкою "Пошук". За замовчуванням галерея показує фотографії природи (nature).

Горизонтальна навігація категорій дозволяє швидко переключатися між популярними темами: Featured, Wallpapers, 3D Renders, Nature, Textures, Film, Architecture, Street Photography, Experimental, Travel, People. Категорії можна прокручувати горизонтально.

Фільтри сортування надають вибір між трьома режимами:
- Релевантність (за замовчуванням) - фото що найбільше відповідають запиту
- Від найпопулярніших ⬇️ - спочатку фото з найбільшою кількістю лайків
- Від найновіших 🕒 - найсвіжіші фотографії

Три перемикача режимів завантаження:
- 📄 Пагінація - класична навігація сторінками
- ➕ Завантажити більше - кнопка для ручного довантаження
- ∞ Infinite Scroll - автоматичне завантаження (активний за замовчуванням)

**[СКРІН 2: Структура проєкту в VS Code]**

*(Має бути зображення дерева файлів проєкту:*
*- папки: public/, src/ (config.js, state.js, dom.js, storage.js, api.js, ui.js, events.js, main.js, style.css)*
*- файли: .gitignore, index.html, package.json*
*- модульна архітектура з розділенням відповідальностей)*

## 4.2 Пошук фотографій

Користувач може шукати фотографії за будь-якими ключовими словами англійською мовою:

1. Клік на поле пошуку для фокусування.
2. Введення ключового слова (наприклад: ocean, sunset, cat, city, mountain).
3. Натискання кнопки "Пошук" або клавіші Enter.
4. Валідація перевіряє чи поле не порожнє - якщо так, показується повідомлення "Будь ласка, введіть пошуковий запит".
5. Відображається індикатор завантаження (синій spinner з текстом "Завантаження...").
6. Запит відправляється до Unsplash API.
7. Результати відображаються у вигляді адаптивної галереї карток.
8. Автоматично активується вкладка "Всі фото".

**Приклади популярних запитів:**
- Природа: nature, forest, ocean, mountain, sunset, waterfall
- Міста: city, architecture, street, skyline, urban
- Тварини: cat, dog, bird, wildlife, horse
- Абстракція: abstract, pattern, texture, minimal
- Технології: technology, computer, code, workspace

Кожна картка показує:
- Зображення у якості small (400x300px приблизно)
- Ім'я автора з іконкою 📷
- Кількість вподобань з іконкою ❤️
- Кнопку додавання до улюблених 🤍/❤️

**[СКРІН 3: Результати пошуку]**

*(Має бути зображення галереї з результатами пошуку:*
*- форма пошуку з введеним текстом*
*- 9-12 карток фотографій в адаптивній сітці (3 колонки)*
*- кожна картка з автором та лайками*
*- hover ефект на одній з карток)*

**[СКРІН 4: Форма пошуку з валідацією]**

*(Має бути зображення:*
*- червоного alert повідомлення "Будь ласка, введіть пошуковий запит"*
*- порожнього поля пошуку*
*- або процесу введення тексту)*

## 4.3 Швидкий вибір категорій

Горизонтальна навігація з категоріями дозволяє швидко переключатися між популярними темами:

1. Прокручування категорій праворуч-ліворуч (на мобільних - свайпом).
2. Клік на будь-яку категорію (наприклад "Architecture").
3. Категорія стає активною (темний фон, білий текст).
4. Автоматично виконується пошук по цій темі.
5. Галерея оновлюється відповідним контентом.
6. Скидається лічильник сторінок (починається з 1).

Це зручніше ніж вводити текст вручну для популярних тем.

## 4.4 Сортування результатів

Користувач може змінити порядок відображення фотографій:

1. Клік на випадаючий список "Сортування за популярністю".
2. Вибір одного з варіантів:
   - **Релевантність** - Unsplash API сам визначає найкращі відповідності запиту
   - **Від найпопулярніших** - спочатку фото з найбільшою кількістю лайків (тисячі вподобань)
   - **Від найновіших** - найсвіжіші фотографії, недавно завантажені на Unsplash
3. Галерея автоматично перезавантажується з новим сортуванням.
4. Сортування застосовується до всіх наступних завантажень фото.

**Рекомендації використання:**
- Релевантність - для звичайного пошуку
- Популярні - для пошуку найякісніших та найкрасивіших фото
- Найновіші - для пошуку оригінального та унікального контенту

## 4.5 Режими завантаження

Застосунок пропонує три режими завантаження додаткових фотографій:

### Пагінація (📄)

1. Вибір перемикача "Пагінація".
2. Під галереєю з'являється блок з номерами сторінок.
3. Показується: [Попередня] 1 2 [3] 4 5 ... 10 [Наступна]
4. Поточна сторінка виділена синім.
5. Клік на номер сторінки завантажує ці 12 фото та **замінює** всі попередні.
6. Плавна прокрутка до верху сторінки.
7. Віддалені сторінки замінені трикрапкою (...).

**Переваги:** Чіткий контроль, легко повернутись на конкретну сторінку.

**[СКРІН 11: Пагінація з номерами сторінок]**

*(Має бути зображення:*
*- навігації з номерами сторінок*
*- активна сторінка виділена*
*- кнопки "Попередня" та "Наступна"*
*- трикрапка між віддаленими сторінками)*

### Завантажити більше (➕)

1. Вибір перемикача "Завантажити більше".
2. Під галереєю з'являється кнопка "Завантажити більше".
3. Клік на кнопку завантажує наступні 12 фото.
4. Нові фото **додаються** до існуючих (не замінюють).
5. Галерея поступово збільшується.
6. Можна завантажувати необмежену кількість разів.
7. Коли фото закінчуються - кнопка ховається, показується повідомлення "📷 Це всі фото за вашим запитом".

**Переваги:** Контроль завантаження, економія трафіку, не втрачаються попередні фото.

### Infinite Scroll (∞)

1. Вибір перемикача "Infinite Scroll" (активний за замовчуванням).
2. Показується підказка "Прокручуйте вниз для автоматичного завантаження фото ⬇️".
3. Прокручування галереї вниз.
4. За 300 пікселів до кінця автоматично запускається завантаження.
5. Показується spinner завантаження.
6. Нові фото додаються знизу.
7. Можна продовжувати скролити безперервно.
8. Debouncing запобігає надмірним запитам (макс. раз на 200мс).

**Переваги:** Найкращий UX, безперервний перегляд, не потрібно нічого натискати.

**[СКРІН 9: Індикатор завантаження (spinner)]**

*(Має бути зображення:*
*- синього спіннера Bootstrap*
*- тексту "Завантаження..."*
*- посередині сторінки або внизу галереї)*

## 4.6 Перегляд фото у повному розмірі

Детальний перегляд фотографії у модальному вікні:

1. Клік на будь-яку картку фото (крім кнопки улюбленого).
2. Відкривається модальне вікно Bootstrap на повний екран.
3. У вікні відображається:
   - Велике зображення високої якості (regular size, ~1080px ширина)
   - Заголовок з описом фотографії (якщо є)
   - Опис під зображенням
   - **Автор:** Ім'я (@username)
   - Інформація про вподобання
4. Внизу дві кнопки:
   - **Закрити** (сіра) - закриває модальне вікно
   - **Відкрити оригінал** (синя) - відкриває сторінку фото на Unsplash.com у новій вкладці
5. Закрити вікно можна також:
   - Клавішею ESC
   - Кліком поза вікном (на затемнений фон)
   - Кліком на ✕ у правому верхньому куті

**[СКРІН 5: Модальне вікно з фото]**

*(Має бути зображення модального вікна з:*
*- великим зображенням фотографії*
*- заголовком*
*- описом під фото*
*- інформацією про автора*
*- двома кнопками внизу)*

## 4.7 Система улюблених

Збереження та управління улюбленими фотографіями:

### Додавання до улюблених

1. Наведення курсору на картку фото.
2. У правому верхньому куті кнопка з білим серцем 🤍.
3. Клік на серце додає фото до улюблених.
4. Серце стає червоним ❤️ та фон кнопки змінюється на червоний.
5. Лічильник у вкладці "❤️ Улюблені (0)" збільшується на 1.
6. Дані автоматично зберігаються в localStorage браузера.

### Видалення з улюблених

1. Клік на червоне серце ❤️ на улюбленому фото.
2. Серце стає білим 🤍 знову.
3. Лічильник зменшується.
4. Якщо це було на вкладці "Улюблені" - картка зникає з галереї.

### Перегляд улюблених

1. Клік на вкладку "❤️ Улюблені (N)" де N - кількість.
2. Вкладка стає активною (жирний текст, підкреслена).
3. Галерея показує тільки збережені фото.
4. З'являється поле "Мінімум лайків (тільки для улюблених)".
5. Можна фільтрувати: ввести "100" - показуються тільки фото з 100+ лайками.
6. Фільтр працює з debouncing (500мс затримка).
7. Перемикачі режимів завантаження та кнопка "Завантажити більше" ховаються (працює тільки для вкладки "Всі фото").

### Збереження даних

- Всі улюблені зберігаються в localStorage браузера.
- Дані зберігаються навіть після:
  - Закриття вкладки
  - Закриття браузера
  - Перезавантаження комп'ютера
- Дані НЕ синхронізуються між пристроями (локально для кожного браузера).
- Очищення даних сайту в налаштуваннях браузера видалить улюблені.

**[СКРІН 6: Вкладка "Улюблені"]**

*(Має бути зображення:*
*- активної вкладки "❤️ Улюблені (5)"*
*- галереї збережених фото*
*- всі кнопки улюблених червоні ❤️*
*- поля фільтра "Мінімум лайків")*

**[СКРІН 12: localStorage в DevTools]**

*(Має бути зображення Chrome DevTools:*
*- вкладка Application*
*- розділ Local Storage*
*- ключ "favorites"*
*- значення: JSON масив з об'єктами фото)*

## 4.8 Адаптивний дизайн

Застосунок коректно відображається на всіх пристроях:

### Великі екрани (Desktop)
- Галерея: 3 колонки фотографій
- Всі елементи розташовані горизонтально
- Категорії прокручуються мишкою або трекпадом
- Hover ефекти на картках (підняття, тінь)

### Середні екрани (Tablet)
- Галерея: 2 колонки
- Форма пошуку та фільтри в два ряди
- Категорії свайпаються пальцем

### Малі екрани (Mobile)
- Галерея: 1 колонка
- Форма пошуку на всю ширину
- Кнопка пошуку під полем введення
- Вкладки та перемикачі режимів вертикально
- Модальне вікно на весь екран
- Оптимізовані розміри кнопок для пальців

**[СКРІН 8: Адаптивний дизайн на мобільному]**

*(Має бути зображення мобільної версії:*
*- вузький екран (375px ширина)*
*- одна колонка фотографій*
*- форма пошуку на всю ширину*
*- вертикальні елементи управління)*

## 4.9 Обробка помилок та граничних випадків

Застосунок коректно обробляє всі можливі помилки:

### Помилки валідації
- **Порожній запит:** Червоний alert "Будь ласка, введіть пошуковий запит"
- Показується відразу при спробі пошуку
- Alert автоматично ховається при наступному запиті

**[СКРІН 7: Повідомлення про помилку валідації]**

*(Має бути зображення:*
*- червоного Bootstrap alert*
*- текст помилки валідації*
*- під формою пошуку)*

### Помилки API

**401 Unauthorized:**
- "Невірний API ключ. Будь ласка, налаштуйте UNSPLASH_ACCESS_KEY в config.js"
- Виникає при неправильному або відсутньому ключі
- Розробнику потрібно оновити ключ у файлі конфігурації

**403 Forbidden:**
- "Перевищено ліміт запитів до API. Спробуйте пізніше."
- Unsplash має ліміт 50 запитів на годину для демо ключів
- Рекомендується зачекати або використати власний API ключ з вищим лімітом

**Інші помилки сервера:**
- "Помилка API: 404" (або інший код статусу)
- Загальна помилка для непередбачуваних ситуацій

**[СКРІН 10: Повідомлення про помилку API]**

*(Має бути зображення:*
*- червоного alert з помилкою API*
*- текст "Перевищено ліміт запитів..."*
*- або "Помилка мережі...")*

### Помилки мережі

**Відсутнє з'єднання:**
- "Помилка мережі. Перевірте з'єднання з інтернетом."
- Виникає коли немає інтернету або заблоковано запити
- Користувачу рекомендується перевірити Wi-Fi/дані

### Помилки localStorage

**Переповнення/недоступність:**
- "Не вдалося зберегти улюблене фото"
- Консольне повідомлення для розробника
- У приватному режимі деяких браузерів localStorage недоступний

### Граничні випадки

**Відсутні результати пошуку:**
- Великий Empty state з іконкою камери 📷
- "Нічого не знайдено"
- "Спробуйте інший пошуковий запит"
- Рекомендація спробувати інші ключові слова

**Кінець результатів:**
- У режимах "Завантажити більше" та "Infinite Scroll"
- Показується повідомлення "📷 Це всі фото за вашим запитом"
- Попередні фото **залишаються** на екрані
- Кнопки завантаження ховаються

**Порожні улюблені:**
- Empty state на вкладці "Улюблені"
- Підказка додати фото до улюблених
- Лічильник показує (0)

\newpage

# 5. СТРУКТУРА ПРОЄКТУ

## 5.1 Модульна архітектура

Проєкт організовано за принципом розділення відповідальностей з використанням ES6 модулів:

```
photo-gallery/
├── public/
│   └── vite.svg                 # Іконка Vite
├── src/
│   ├── config.js                # Конфігурація API
│   ├── state.js                 # Глобальний стан застосунку
│   ├── dom.js                   # Посилання на DOM елементи
│   ├── storage.js               # Робота з localStorage
│   ├── api.js                   # HTTP запити до Unsplash API
│   ├── ui.js                    # Функції відображення UI
│   ├── events.js                # Обробники подій
│   ├── main.js                  # Точка входу, ініціалізація
│   └── style.css                # Власні стилі
├── .git/                        # Git репозиторій
├── .gitignore                   # Виключення для Git
├── index.html                   # HTML структура
├── package.json                 # Залежності та скрипти
├── package-lock.json            # Locked версії залежностей
└── README.md                    # Документація проєкту
```

**[СКРІН 18: Код проєкту у VS Code]**

*(Має бути зображення VS Code з:*
*- відкритим файлом main.js або events.js*
*- деревом файлів зліва з модульною структурою*
*- підсвіткою синтаксису JavaScript*
*- номерами рядків)*

## 5.2 Опис модулів

### config.js
Містить константи конфігурації:
- `UNSPLASH_ACCESS_KEY` - ключ доступу до API
- `UNSPLASH_API_URL` - базовий URL API

### state.js
Глобальний стан застосунку:
- `currentPage` - поточна сторінка пагінації
- `currentQuery` - поточний пошуковий запит
- `photos` - масив завантажених фотографій
- `currentTab` - активна вкладка (all/favorites)
- `isLoading` - прапорець завантаження
- `loadingMode` - режим завантаження (pagination/loadMore/infinite)
- `totalPages` - загальна кількість сторінок
- `sortOrder` - порядок сортування (relevant/popular/latest)
- `minLikes` - мінімальна кількість лайків для фільтра

### dom.js
Посилання на всі DOM елементи:
- Форма пошуку та елементи введення
- Галерея та контейнери
- Модальне вікно та його елементи
- Вкладки та кнопки
- Фільтри та перемикачі

### storage.js
Функції роботи з localStorage:
- `getFavorites()` - отримання улюблених
- `saveFavorites()` - збереження улюблених
- `isFavorite()` - перевірка чи фото в улюблених
- `toggleFavorite()` - додати/видалити улюблене
- `updateFavoritesCount()` - оновлення лічильника

### api.js
Функції для роботи з API:
- `fetchPhotos(query, page)` - завантаження фотографій
- Обробка помилок API
- Управління станом завантаження

### ui.js
Функції відображення UI:
- `createPhotoCard()` - створення картки фото
- `displayPhotos()` - відображення галереї
- `showPhotoModal()` - показ модального вікна
- `renderPagination()` - відображення пагінації
- `showLoading()` / `hideError()` - індикатори стану

### events.js
Обробники подій:
- `setupSearchForm()` - пошук
- `setupGalleryClick()` - кліки по галереї
- `setupInfiniteScroll()` - нескінченний скрол
- `setupTabs()` - перемикання вкладок
- `setupCategories()` - вибір категорій
- `setupLoadingModeToggle()` - режими завантаження
- `setupSortFilter()` - сортування
- `setupLikesFilter()` - фільтр лайків
- `setupEventHandlers()` - ініціалізація всіх обробників

### main.js
Точка входу застосунку:
- Імпорт всіх модулів
- Функція `init()` для ініціалізації
- Завантаження початкових фотографій

### style.css
Власні стилі:
- Reset та базові стилі
- Стилізація карток фотографій
- Кнопка улюбленого
- Анімації (fadeIn, bounce)
- Категорії та фільтри
- Адаптивність (@media queries)

## 5.3 Залежності проєкту

Файл `package.json`:

```json
{
  "name": "photo-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bootstrap": "^5.3.8"
  }
}
```

**Production залежності:**
- **axios** - HTTP клієнт для запитів до API
- **bootstrap** - UI фреймворк для стилізації

**Dev залежності:**
- **vite** - швидкий збирач та dev-сервер

## 5.4 Git конфігурація

Файл `.gitignore`:

```
# Залежності
node_modules

# Результати збірки
dist
dist-ssr

# Локальні файли редактора
*.local
.vscode/*
!.vscode/extensions.json

# Системні файли
.DS_Store
```

\newpage

# 6. ІНСТРУКЦІЯ З ВИКОРИСТАННЯ

## 6.1 Встановлення проєкту

### Крок 1: Клонування репозиторію

Відкрити термінал та виконати:

```bash
git clone https://github.com/AlexBram003/Practika.git
cd Practika
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

Буде встановлено:
- vite@7.2.4 - збирач та dev-сервер
- axios@1.13.2 - HTTP клієнт
- bootstrap@5.3.8 - UI фреймворк

### Крок 3: Налаштування API ключа

1. Перейти на https://unsplash.com/developers
2. Зареєструватись або увійти в акаунт
3. Натиснути "New Application"
4. Прийняти умови використання
5. Заповнити форму заявки:
   - Application name: Photo Gallery
   - Description: Educational project for practice
6. Скопіювати "Access Key"
7. Відкрити файл `src/config.js`
8. Замінити значення:

```javascript
export const UNSPLASH_ACCESS_KEY = 'ваш_скопійований_ключ_тут'
```

### Крок 4: Запуск development сервера

```bash
npm run dev
```

У терміналі з'явиться:

```
VITE v7.2.4  ready in 234 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

Відкрити браузер та перейти на http://localhost:5173

**[СКРІН 19: Термінал з запущеним dev-сервером]**

*(Має бути зображення терміналу:*
*- команда "npm run dev"*
*- вивід Vite з версією*
*- "Local: http://localhost:5173/"*
*- "ready in XXX ms")*

## 6.2 Використання застосунку

### Пошук фотографій

1. Ввести ключове слово англійською (nature, city, cat)
2. Натиснути "Пошук" або Enter
3. Почекати завантаження
4. Переглядати результати у галереї

### Вибір категорії

1. Прокрутити горизонтальні категорії
2. Клікнути на цікаву (наприклад, Architecture)
3. Галерея автоматично оновиться

### Зміна сортування

1. Відкрити список "Сортування за популярністю"
2. Вибрати потрібний варіант
3. Результати перезавантажаться

### Вибір режиму завантаження

1. Клікнути один з трьох перемикачів:
   - Пагінація - для навігації сторінками
   - Завантажити більше - для ручного контролю
   - Infinite Scroll - для безперервного перегляду

### Додавання до улюблених

1. Навести на фото
2. Клікнути біле серце 🤍 у верхньому правому куті
3. Серце стане червоним ❤️
4. Перевірити збільшення лічильника у вкладці "Улюблені"

### Перегляд у повному розмірі

1. Клікнути на будь-яке фото
2. Переглянути деталі у модальному вікні
3. За бажанням натиснути "Відкрити оригінал" для Unsplash
4. Закрити натисканням "Закрити" або ESC

### Перегляд улюблених

1. Клікнути вкладку "❤️ Улюблені"
2. Побачити всі збережені фото
3. За бажанням ввести мінімум лайків для фільтрації
4. Видалити фото натисканням червоного серця

## 6.3 Збірка для production

### Створення production збірки

```bash
npm run build
```

Результат:

```
vite v7.2.4 building client environment for production...
✓ 120 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                   6.89 kB │ gzip:  2.22 kB
dist/assets/index-DA-2HaLX.css  234.09 kB │ gzip: 31.50 kB
dist/assets/index-CYKJ_qMz.js   130.41 kB │ gzip: 42.56 kB
✓ built in 1.24s
```

Оптимізована збірка буде в папці `dist/`

### Попередній перегляд збірки

```bash
npm run preview
```

Відкриє production версію на http://localhost:4173

### Deployment

Папку `dist/` можна розмістити на будь-якому статичному хостингу:
- GitHub Pages
- Netlify
- Vercel
- Firebase Hosting

\newpage

# 7. ВИСНОВКИ

В результаті виконання навчальної практики з розробки WEB-застосунків було створено повнофункціональний веб-застосунок **Photo Gallery** з використанням сучасних технологій та інструментів.

## Досягнуті результати

**Всі 10 тем практики повністю реалізовані:**

1. ✅ **Створення та налаштування WEB-проєкту** - ініціалізовано проєкт з Vite, налаштовано Git репозиторій, створено модульну структуру з розділенням файлів за відповідальністю (config, state, dom, storage, api, ui, events).

2. ✅ **Робота з DOM-деревом** - реалізовано вибір елементів через getElementById та querySelectorAll, динамічне створення карток фотографій через createElement та innerHTML, ефективне управління класами через classList API.

3. ✅ **Обробка подій користувача** - створено обробники для submit форми, click на галереї з делегуванням подій, scroll для infinite scroll, зміни фільтрів та режимів завантаження.

4. ✅ **Робота з масивами та об'єктами** - активно використовуються методи forEach, find, some, findIndex, splice, filter та spread оператор для обробки колекцій фотографій та улюблених.

5. ✅ **Розробка HTML-форм та валідація** - створено форми пошуку та фільтрації з HTML5 атрибутами (required) та додатковою JavaScript валідацією через trim() та перевірки на порожні значення.

6. ✅ **Використання Bootstrap та Axios** - інтегровано Bootstrap 5.3.8 для UI компонентів (Navbar, Card, Modal, Tabs, Grid), Axios для HTTP запитів з автоматичним парсингом JSON.

7. ✅ **Взаємодія з зовнішніми API** - повна інтеграція з Unsplash API включаючи пошук, пагінацію, сортування (relevant/popular/latest), обробку JSON відповідей з вкладеними об'єктами.

8. ✅ **Обробка помилок у JavaScript** - детальна обробка через try-catch блоки, async/await синтаксис, розрізнення типів помилок (response/request/інші), зрозумілі повідомлення користувачу українською мовою.

9. ✅ **Пагінація та нескінченний скрол** - реалізовано три режими: класична пагінація з номерами сторінок, кнопка "Завантажити більше", infinite scroll з debouncing (200мс) та перевіркою позиції скролу.

10. ✅ **Використання localStorage** - повноцінна система улюблених з збереженням через JSON.stringify/parse, функції читання/запису з обробкою помилок, фільтрація за мінімальними лайками.

**Додаткові досягнення понад вимоги:**

- Горизонтальна навігація з категоріями для швидкого доступу до популярних тем
- Система сортування фотографій (релевантність, популярність, новизна)
- Три режими завантаження з перемикачем для різних сценаріїв використання
- Фільтрація улюблених фотографій за мінімальною кількістю лайків
- Інтелектуальна пагінація з трикрапкою для віддалених сторінок
- Коректна обробка кінця результатів без втрати попередніх фото
- Повідомлення "Це всі фото за вашим запитом" замість порожньої галереї
- Модульна архітектура з 8 окремих файлів для різної функціональності
- Використання Bootstrap класів (d-none) замість inline стилів
- Адаптивний дизайн для desktop, tablet та mobile
- Плавні анімації та переходи (fadeIn, bounce, smooth scroll)
- Чистий та зрозумілий код з українськими коментарями
- Повна документація у README.md

**[СКРІН 20: Фінальний вигляд застосунку]**

*(Має бути зображення повного екрану:*
*- навігація "📸 Photo Gallery"*
*- форма пошуку*
*- горизонтальні категорії*
*- фільтри сортування*
*- вкладки та режими*
*- заповнена галерея з 12 фото*
*- всі елементи інтерфейсу)*

## Набуті навички

**Технічні навички:**

- Робота з Vite як сучасним інструментом збірки та dev-сервером
- Використання Axios для HTTP запитів з обробкою помилок
- Інтеграція Bootstrap 5 для створення адаптивного UI
- Робота з Unsplash API та обробка JSON структур
- Управління станом застосунку через централізований об'єкт state
- Git для контролю версій з правильним .gitignore
- Налагодження в Chrome DevTools (Application, Console, Network)
- ES6+ синтаксис (модулі, стрілкові функції, деструктуризація, spread)

**Методологічні навички:**

- Модульна архітектура з розділенням відповідальностей (SRP)
- Асинхронне програмування через async/await та promises
- Обробка помилок на всіх рівнях (валідація, мережа, API, localStorage)
- Оптимізація продуктивності (debouncing, перевірки isLoading)
- Event delegation для ефективної обробки подій
- Immutable підхід до роботи з даними (spread замість мутацій)
- Документування коду та створення README
- Написання зрозумілих commit messages

**UX/UI навички:**

- Створення інтуїтивно зрозумілого інтерфейсу
- Надання користувачу вибору (3 режими завантаження)
- Візуальний feedback (спінери, alerts, анімації)
- Адаптивний дизайн для різних пристроїв
- Accessibility (семантичний HTML, aria-атрибути)

## Практична цінність

Створений застосунок демонструє:

- **Реальну інтеграцію з зовнішнім API** - не mock дані, а справжні фотографії з Unsplash
- **Сучасні підходи до веб-розробки** - ES6 модулі, async/await, компонентний підхід
- **Best practices JavaScript** - чистий код, обробка помилок, оптимізація
- **Продуманий UX** - три режими завантаження, фільтри, сортування, улюблені
- **Готовність до використання** - можна розгорнути на хостингу та використовувати

**Можливості для розширення:**

- Автентифікація користувачів для синхронізації улюблених між пристроями
- Створення колекцій фотографій (альбоми)
- Завантаження фотографій у високій якості
- Соціальний функціонал (поділитись фото)
- Пошук за кольором або орієнтацією
- Історія пошуку
- Backend для збереження даних

Проєкт **повністю відповідає** методичним рекомендаціям практики та охоплює всі обов'язкові теми з детальними прикладами реалізації та демонстрацією функціональності. Застосунок готовий до presentation та може служити портфоліо проєктом для демонстрації навичок веб-розробки.

\newpage

# 8. СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ

## Офіційна документація

1. **Unsplash API Documentation**
   https://unsplash.com/documentation
   Документація API Unsplash для роботи з фотографіями

2. **Vite Documentation**
   https://vitejs.dev/guide/
   Офіційна документація збирача Vite

3. **Bootstrap 5 Documentation**
   https://getbootstrap.com/docs/5.3/
   Документація фреймворку Bootstrap 5

4. **Axios Documentation**
   https://axios-http.com/docs/intro
   Документація бібліотеки Axios для HTTP запитів

5. **MDN Web Docs - JavaScript**
   https://developer.mozilla.org/en-US/docs/Web/JavaScript
   Повний довідник JavaScript

6. **MDN Web Docs - HTML**
   https://developer.mozilla.org/en-US/docs/Web/HTML
   Документація HTML

7. **MDN Web Docs - CSS**
   https://developer.mozilla.org/en-US/docs/Web/CSS
   Документація CSS

8. **MDN Web Docs - Web Storage API**
   https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API
   Документація localStorage та sessionStorage

## Навчальні матеріали

9. **JavaScript.info** - Сучасний підручник JavaScript
   https://uk.javascript.info/
   Детальний курс JavaScript українською мовою

10. **W3Schools - JavaScript Tutorial**
    https://www.w3schools.com/js/
    Інтерактивні уроки JavaScript

11. **Eloquent JavaScript** (4th Edition) - Marijn Haverbeke
    https://eloquentjavascript.net/
    Книга про JavaScript з прикладами

## Методичні рекомендації

12. **Навчальна практика з розробки WEB-застосунків**
    Методичні рекомендації щодо виконання практичних завдань
    Олександрійський політехнічний фаховий коледж, 2025

## Інструменти розробки

13. **Git Documentation**
    https://git-scm.com/doc
    Документація системи контролю версій Git

14. **NPM Documentation**
    https://docs.npmjs.com/
    Документація менеджера пакетів npm

15. **Chrome DevTools**
    https://developer.chrome.com/docs/devtools/
    Інструменти розробника Chrome

16. **Visual Studio Code**
    https://code.visualstudio.com/docs
    Документація редактора VS Code
