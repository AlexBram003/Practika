# 📸 Photo Gallery - Галерея Фотографій

**Варіант 6** - Навчальна практика з розробки WEB-застосунків

## 📋 Опис проєкту

Photo Gallery - це веб-застосунок для перегляду фотографій з API Unsplash. Застосунок дозволяє шукати фото за ключовими словами, переглядати їх у повному розмірі, зберігати улюблені фото та завантажувати більше результатів.

## ✨ Основні функції

- 🔍 **Пошук фотографій** за ключовими словами
- 📱 **Адаптивний дизайн** з використанням Bootstrap
- 🖼️ **Модальне вікно** для перегляду фото у повному розмірі
- ❤️ **Улюблені фото** зі збереженням у localStorage
- 📄 **Пагінація** - завантаження додаткових фото
- 🎨 **Анімації** та плавні переходи
- ⚡ **Швидке завантаження** з lazy loading для зображень

## 🛠️ Технології

- **Vite** - швидкий збирач модулів
- **JavaScript (ES6+)** - сучасний синтаксис
- **Axios** - для HTTP запитів до API
- **Bootstrap 5** - для адаптивного інтерфейсу
- **Unsplash API** - джерело фотографій
- **localStorage** - для збереження улюблених фото

## 📚 Реалізовані теми практики

### 1. Створення та налаштування WEB-проєкту
- ✅ Ініціалізація проєкту з Vite
- ✅ Налаштування package.json та залежностей
- ✅ Структура проєкту (HTML, CSS, JS)

### 2. Робота з DOM-деревом
- ✅ Вибір елементів (querySelector, getElementById)
- ✅ Динамічне створення елементів (createElement)
- ✅ Зміна вмісту та атрибутів елементів

### 3. Обробка подій користувача
- ✅ Обробка submit форми пошуку
- ✅ Обробка кліків (делегування подій)
- ✅ Інтерактивні елементи (кнопки, картки)

### 4. Робота з масивами та об'єктами
- ✅ Методи масивів: `map`, `filter`, `find`, `forEach`
- ✅ Робота з об'єктами (деструктуризація, spread operator)
- ✅ Перетворення та фільтрація даних

### 5. Робота з HTML-формами
- ✅ Валідація введення (required)
- ✅ Обробка відправки форми
- ✅ Зчитування даних з input полів

### 6. Використання Bootstrap та Axios
- ✅ Підключення Bootstrap для стилізації
- ✅ Використання компонентів (Card, Modal, Navbar, Tabs)
- ✅ HTTP-запити через Axios

### 7. Взаємодія з зовнішніми API
- ✅ GET-запити до Unsplash API
- ✅ Обробка JSON відповідей
- ✅ Параметри запитів (query, page, per_page)

### 8. Обробка помилок у JavaScript
- ✅ Try-catch блоки
- ✅ Async/await синтаксис
- ✅ Обробка різних типів помилок (мережа, API, валідація)

### 9. Пагінація та завантаження даних
- ✅ Кнопка "Завантажити ще"
- ✅ Відстеження поточної сторінки
- ✅ Додавання нових елементів до DOM

### 10. Використання localStorage
- ✅ Збереження улюблених фото
- ✅ Читання даних при завантаженні
- ✅ Видалення з localStorage

### 11. Інструменти налагодження
- ✅ Console для виводу помилок
- ✅ Обробка та відображення помилок для користувача

### 12. Підготовка до публікації
- ✅ Оптимізація коду
- ✅ README документація
- ✅ Команди для збірки проєкту

## 🚀 Встановлення та запуск

### Передумови

- Node.js (версія 16 або вища)
- npm (менеджер пакетів)

### Крок 1: Клонування репозиторію

```bash
git clone <repository-url>
cd photo-gallery
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

### Крок 3: Налаштування API ключа

1. Зареєструйтесь на [Unsplash Developers](https://unsplash.com/developers)
2. Створіть новий додаток
3. Скопіюйте Access Key
4. Відкрийте файл `src/main.js`
5. Замініть `YOUR_ACCESS_KEY_HERE` на ваш Access Key:

```javascript
const UNSPLASH_ACCESS_KEY = 'your_actual_access_key_here'
```

### Крок 4: Запуск проєкту

```bash
npm run dev
```

Проєкт буде доступний за адресою: `http://localhost:5173`

## 📦 Команди npm

- `npm run dev` - запуск dev-сервера
- `npm run build` - збірка для production
- `npm run preview` - попередній перегляд production збірки

## 📁 Структура проєкту

```
photo-gallery/
├── public/              # Статичні файли
├── src/
│   ├── main.js         # Головний JavaScript файл
│   └── style.css       # Стилі CSS
├── index.html          # HTML структура
├── package.json        # Залежності проєкту
├── vite.config.js      # Конфігурація Vite
└── README.md           # Документація
```

## 🎯 Як користуватися

1. **Пошук фото**: Введіть ключове слово у поле пошуку (наприклад, "nature", "cat", "city")
2. **Перегляд фото**: Клікніть на будь-яке фото для перегляду у повному розмірі
3. **Улюблені**: Натисніть на іконку серця, щоб додати фото до улюблених
4. **Вкладка улюблених**: Перейдіть на вкладку "Улюблені" для перегляду збережених фото
5. **Завантаження більше**: Натисніть кнопку "Завантажити ще" для додаткових результатів

## 🔧 Особливості реалізації

### Робота з API

```javascript
// Приклад асинхронного запиту з обробкою помилок
async function fetchPhotos(query, page) {
  try {
    const response = await axios.get(url, { params })
    return response.data.results
  } catch (error) {
    // Детальна обробка помилок
    if (error.response) {
      // Помилка від сервера
    } else if (error.request) {
      // Помилка мережі
    }
  }
}
```

### LocalStorage

```javascript
// Збереження улюблених
function saveFavorites(favorites) {
  localStorage.setItem('favorites', JSON.stringify(favorites))
}

// Читання улюблених
function getFavorites() {
  const favorites = localStorage.getItem('favorites')
  return favorites ? JSON.parse(favorites) : []
}
```

### DOM маніпуляції

```javascript
// Динамічне створення карток
function createPhotoCard(photo) {
  const col = document.createElement('div')
  col.innerHTML = `<div class="photo-card">...</div>`
  return col
}
```

## 🎨 Дизайн

- Адаптивна сітка Bootstrap (responsive grid)
- Hover ефекти на картках
- Плавні анімації (CSS transitions)
- Модальне вікно Bootstrap для повноекранного перегляду

## 🐛 Обробка помилок

Застосунок обробляє наступні типи помилок:

- ❌ Невірний API ключ (401)
- ❌ Перевищення ліміту запитів (403)
- ❌ Помилки мережі
- ❌ Порожні пошукові запити
- ❌ Помилки localStorage

## 📝 Автор

Студент групи ІV курс, ОП-221
Олександрійський політехнічний фаховий коледж
Навчальна практика з розробки WEB-застосунків

## 📄 Ліцензія

Цей проєкт створено в навчальних цілях.

## 🔗 Корисні посилання

- [Unsplash API Documentation](https://unsplash.com/documentation)
- [Vite Documentation](https://vitejs.dev/)
- [Bootstrap Documentation](https://getbootstrap.com/)
- [Axios Documentation](https://axios-http.com/)
- [MDN Web Docs - JavaScript](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
