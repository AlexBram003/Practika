---
title: "ЗВІТ З НАВЧАЛЬНОЇ ПРАКТИКИ"
geometry: "left=2cm,right=1cm,top=2cm,bottom=2cm"
fontsize: 14pt
mainfont: "Times New Roman"
documentclass: article
linestretch: 1.5
indent: true
---

\begin{titlepage}
\centering

{\Large ОЛЕКСАНДРІЙСЬКИЙ ПОЛІТЕХНІЧНИЙ ФАХОВИЙ КОЛЕДЖ}

\vspace{1cm}

Циклова комісія комп'ютерних дисциплін

\vspace{3cm}

{\Large\textbf{ЗВІТ}}

\vspace{0.5cm}

з навчальної практики

\vspace{0.5cm}

з орієнтованої розробки WEB-застосунків

\vspace{0.5cm}

НП 121.221.010

\vspace{2cm}

\begin{flushleft}
Студента ІV курсу ОП-221 групи

Освітньо-професійної програми

«Інженерія програмного забезпечення»

Олександра БРАМА

\vspace{1cm}

Керівники викладачі спецдисциплін,

Едуард БОЙКО

Андрій КОРНІЄНКО

\vspace{1cm}

Оцінка \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_
\end{flushleft}

\vfill

м. Олександрія

2026 рік

\end{titlepage}

\newpage

# ЗМІСТ

1. ВСТУП ........................................ 3
2. СТВОРЕННЯ ТА ПОЧАТКОВЕ НАЛАШТУВАННЯ WEB-ПРОЄКТУ: VITE, GIT, HTML-СТРУКТУРА .............. 5
3. РОБОТА З DOM-ДЕРЕВОМ ТА ДИНАМІЧНЕ ОНОВЛЕННЯ ІНТЕРФЕЙСУ ................................. 8
4. ОБРОБКА ПОДІЙ КОРИСТУВАЧА В DOM-ДЕРЕВІ ...... 10
5. РОБОТА З МАСИВАМИ ТА ОБ'ЄКТАМИ В JavaScript . 12
6. РОЗРОБКА HTML-ФОРМ ТА ВАЛІДАЦІЯ ДАНИХ ....... 14
7. ВИКОРИСТАННЯ BOOTSTRAP ТА AXIOS: АДАПТИВНИЙ ІНТЕРФЕЙС І ПЕРШІ ЗАПИТИ ДО API ............ 16
8. ВЗАЄМОДІЯ З ЗОВНІШНІМИ API: HTTP-ЗАПИТИ, FETCH/AXIOS ТА ОБРОБКА JSON .................. 18
9. ОБРОБКА ПОМИЛОК У JAVASCRIPT ТА ПІД ЧАС МЕРЕЖЕВИХ ЗАПИТІВ .............................. 21
10. ПАГІНАЦІЯ ТА НЕСКІНЧЕННИЙ СКРОЛ У WEB-ЗАСТОСУНКАХ .................................. 23
11. ВИКОРИСТАННЯ LOCALSTORAGE ТА КЕШУВАННЯ ДАНИХ НА КЛІЄНТІ .............................. 25
12. ІНСТРУМЕНТИ НАЛАГОДЖЕННЯ JAVASCRIPT-КОДУ ... 27
13. ПІДГОТОВКА WEB-ЗАСТОСУНКУ ДО ПУБЛІКАЦІЇ ТА РОЗГОРТАННЯ ................................... 29
14. ПРЕЗЕНТАЦІЯ ПРОЄКТУ ......................... 31
ВИСНОВКИ ........................................ 32
ПЕРЕЛІК ВИКОРИСТАНИХ ДЖЕРЕЛ .................... 33

\newpage

center
Змн.
Арк.
№ докум.
Підпис
Дата
Арк.
2
НПРВЗ 121.221.010 ПЗ
 Розроб.
Брам О.О.
 програм з використанням оператора циклу з параметром

 Перевір.
Бойко Е.О.

 Н. Контр.
 Затверд.
Звіт з навчальної
практики з розробки
WEB-застосунків
Літ.
Аркушів
35
ОПФК, ОП-221

\newpage

# ВСТУП

Сучасний світ активно розвивається в напрямку цифрових технологій, які охоплюють майже всі сфери діяльності людини. Веб-технології сьогодні широко використовуються в бізнесі, освіті, сфері послуг та повсякденному житті. У зв'язку з цим професія веб-розробника є затребуваною, а рівень вимог до майбутніх фахівців у галузі інформаційних технологій постійно зростає [3]. Для студента, який планує працювати в IT-сфері, важливо не лише володіти теоретичними знаннями, а й мати практичні напрацювання. Одним із таких практичних прикладів є створення власного веб-застосунку, який дозволяє продемонструвати набуті навички та представити себе потенційному роботодавцю.

Тема навчальної практики – розробка веб-застосунку «Photo Gallery» – є актуальною, оскільки дає можливість на практиці закріпити знання з веб-розробки та пройти основні етапи створення веб-сайту [1]. У процесі виконання роботи відбувається проєктування структури застосунку, розробка інтерфейсу, верстка сторінок, реалізація інтерактивних елементів і підготовка ресурсу до публікації в мережі Інтернет. Це дозволяє отримати практичний досвід, який є важливим для подальшої професійної діяльності.

Основою створення веб-застосунку є мова гіпертекстової розмітки HTML. У даній роботі використовується стандарт HTML5, який забезпечує можливість семантичної розмітки сторінок. Застосування таких тегів, як &lt;header&gt;, &lt;nav&gt;, &lt;section&gt; та &lt;footer&gt;, дозволяє логічно структурувати контент і покращити його сприйняття користувачами, а також позитивно впливає на доступність та індексацію сайту пошуковими системами [4; 9].

Для оформлення зовнішнього вигляду веб-ресурсу використовується CSS3. Сучасна верстка передбачає застосування технологій Flexbox та CSS Grid, які дозволяють створювати адаптивні макети сторінок [5]. Адаптивний дизайн забезпечує коректне відображення сайту на різних пристроях, зокрема на смартфонах і планшетах, що є важливою вимогою сучасних веб-ресурсів. Також у роботі використовуються медіа-запити для зміни стилів залежно від розміру екрана [9].

Інтерактивність веб-застосунку реалізується за допомогою мови програмування JavaScript відповідно до стандарту ECMAScript 6+. JavaScript використовується для обробки подій користувача, динамічного оновлення галереї фотографій та валідації форм пошуку. Клієнтська валідація дозволяє оперативно повідомляти користувача про помилки введення даних і покращує зручність користування застосунком [6; 10].

Під час розробки проєкту застосовувалися сучасні інструменти веб-розробки, зокрема збирач модулів Vite, редактор коду Visual Studio Code та система керування версіями Git. Використання Git дозволяє зберігати історію змін проєкту та працювати з віддаленими репозиторіями, наприклад GitHub, що є стандартною практикою у сфері розробки програмного забезпечення [7].

Метою даної роботи є створення веб-застосунку Photo Gallery для пошуку та перегляду фотографій з API Unsplash. Розроблений веб-ресурс повинен наочно представляти фотографії за пошуковими запитами користувача у зручному інтерфейсі. Для досягнення поставленої мети необхідно виконати такі завдання:

- розробити структуру застосунку та створити адаптивний інтерфейс [8];
- виконати семантичну верстку та реалізувати адаптивне оформлення;
- додати інтерактивні елементи за допомогою JavaScript;
- реалізувати взаємодію з зовнішнім API Unsplash;
- додати функціонал збереження улюблених фотографій через localStorage;
- перевірити коректність роботи веб-застосунку в різних браузерах.

У результаті виконання роботи планується отримати повноцінний веб-застосунок, який має просту та зрозумілу структуру, що дозволяє користувачеві швидко знайти необхідні фотографії. Отримані під час виконання практики навички можуть бути використані у подальшому навчанні та професійній діяльності.

\newpage

# 1 СТВОРЕННЯ ТА ПОЧАТКОВЕ НАЛАШТУВАННЯ WEB-ПРОЄКТУ: VITE, GIT, HTML-СТРУКТУРА

Згідно з індивідуальним завданням (Варіант №6), об'єктом розробки є веб-застосунок «Photo Gallery». Метою проєкту є створення застосунку для пошуку та перегляду фотографій з API Unsplash із можливістю збереження улюблених фотографій.

Для технічної реалізації обрано інструмент збірки Vite, який забезпечує швидке налаштування середовища та підтримку ES6 модулів. Ініціалізація проєкту виконана командою npm create vite@latest photo-gallery -- --template vanilla, після чого встановлено залежності (npm install). Також налаштовано систему контролю версій Git: створено локальний репозиторій (git init), додано файли до індексу та виконано початкову фіксацію змін (git commit), що дозволяє відстежувати історію розробки.

У головному файлі index.html сформовано семантичну структуру документа. Використано Bootstrap для створення адаптивного інтерфейсу. Додано навігаційну панель, форму пошуку, галерею фотографій та модальне вікно для перегляду фото у повному розмірі.

Лістинг сформованого коду index.html:

```html
<!doctype html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Photo Gallery - Галерея Фотографій</title>
  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid">
        <span class="navbar-brand mb-0 h1">📸 Photo Gallery</span>
      </div>
    </nav>

    <div class="container my-4">
      <div class="row mb-4">
        <div class="col-12">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Пошук фотографій</h5>
              <form id="searchForm" class="d-flex gap-2">
                <input type="text" id="searchInput"
                       class="form-control"
                       placeholder="Введіть ключове слово..."
                       required />
                <button type="submit" class="btn btn-primary">
                  Пошук
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>

      <div id="photoGallery" class="row row-cols-1 row-cols-md-3 g-4">
      </div>
    </div>

    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

Файл package.json з налаштуваннями проєкту:

```json
{
  "name": "photo-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bootstrap": "^5.3.8"
  }
}
```

Ініціалізація Git репозиторію:

```bash
git init
git add .
git commit -m "Початкова ініціалізація проєкту Photo Gallery"
```

**[СКРІН 1: Структура проєкту в VS Code]**

*(Дерево файлів проєкту з папками src/, public/, файлами index.html, package.json)*

\newpage

# 2 РОБОТА З DOM-ДЕРЕВОМ ТА ДИНАМІЧНЕ ОНОВЛЕННЯ ІНТЕРФЕЙСУ

У Photo Gallery реалізовано систему динамічного створення карток фотографій. Створено централізований об'єкт з посиланнями на всі DOM елементи в файлі dom.js. Картки фотографій створюються динамічно при отриманні даних з API та додаються до галереї через appendChild().

Для показу/приховування елементів використовуються Bootstrap класи через classList API. Кожна картка містить зображення, інформацію про автора, кількість лайків та кнопку улюбленого.

Вибір елементів (src/dom.js):

```javascript
import * as bootstrap from 'bootstrap'

export const elements = {
  searchForm: document.getElementById('searchForm'),
  searchInput: document.getElementById('searchInput'),
  photoGallery: document.getElementById('photoGallery'),
  loadingSpinner: document.getElementById('loadingSpinner'),
  errorAlert: document.getElementById('errorAlert'),
  loadMoreBtn: document.getElementById('loadMoreBtn'),
  allTab: document.getElementById('all-tab'),
  favoritesTab: document.getElementById('favorites-tab'),
  photoModal: new bootstrap.Modal(
    document.getElementById('photoModal')
  ),
  modalPhotoImg: document.getElementById('modalPhotoImg'),
  categoryButtons: document.querySelectorAll('.category-btn')
}
```

Динамічне створення картки фото (src/ui.js):

```javascript
export function createPhotoCard(photo) {
  const col = document.createElement('div')
  col.className = 'col'

  const isFav = isFavorite(photo.id)

  col.innerHTML = `
    <div class="photo-card" data-photo-id="${photo.id}">
      <button class="favorite-btn ${isFav ? 'favorited' : ''}"
              data-photo-id="${photo.id}">
        ${isFav ? '❤️' : '🤍'}
      </button>
      <img src="${photo.urls.small}"
           alt="${photo.alt_description || 'Photo'}"
           loading="lazy" />
      <div class="photo-info">
        <div class="photo-author">📷 ${photo.user.name}</div>
        <div class="photo-likes">❤️ ${photo.likes} вподобань</div>
      </div>
    </div>
  `

  return col
}
```

Додавання до DOM та управління класами:

```javascript
export function displayPhotos(photos, append = false) {
  if (!append) {
    elements.photoGallery.innerHTML = ''
  }

  photos.forEach(photo => {
    const photoCard = createPhotoCard(photo)
    elements.photoGallery.appendChild(photoCard)
  })

  elements.loadMoreBtn.classList.add('d-none')
  elements.scrollHint.classList.remove('d-none')
}
```

**[СКРІН 2: Галерея з динамічно створеними картками фотографій]**

\newpage

# 3 ОБРОБКА ПОДІЙ КОРИСТУВАЧА В DOM-ДЕРЕВІ

В Photo Gallery реалізовано комплексну систему обробки подій. Форма пошуку обробляє submit з preventDefault() для запобігання перезавантаження сторінки. Застосовано паттерн делегування подій для обробки кліків на галереї - один обробник на контейнері замість окремих на кожній картці.

Для нескінченного скролу реалізовано обробку події scroll з debouncing (200мс), що запобігає надмірним запитам до API.

Обробка submit форми (src/events.js):

```javascript
export function setupSearchForm() {
  elements.searchForm.addEventListener('submit', async (e) => {
    e.preventDefault()

    const query = elements.searchInput.value.trim()

    if (!query) {
      showError('Будь ласка, введіть пошуковий запит')
      return
    }

    state.currentQuery = query
    state.currentPage = 1
    state.currentTab = 'all'

    const photos = await fetchPhotos(query, 1)
    state.photos = photos
    displayPhotos(photos)
  })
}
```

Делегування подій для галереї:

```javascript
export function setupGalleryClick() {
  elements.photoGallery.addEventListener('click', (e) => {
    if (e.target.closest('.favorite-btn')) {
      const btn = e.target.closest('.favorite-btn')
      const photoId = btn.dataset.photoId

      let photo = state.photos.find(p => p.id === photoId)
      if (photo) {
        toggleFavorite(photo)
        const isFav = isFavorite(photoId)
        btn.textContent = isFav ? '❤️' : '🤍'
        btn.classList.toggle('favorited', isFav)
      }
    }
    else if (e.target.closest('.photo-card')) {
      const card = e.target.closest('.photo-card')
      const photoId = card.dataset.photoId
      showPhotoModal(photoId)
    }
  })
}
```

Обробка scroll з debouncing:

```javascript
let scrollTimeout

function handleScroll() {
  clearTimeout(scrollTimeout)

  scrollTimeout = setTimeout(async () => {
    if (isBottomReached() &&
        state.currentTab === 'all' &&
        !state.isLoading &&
        state.loadingMode === 'infinite') {
      await loadMorePhotos()
    }
  }, 200)
}

export function setupInfiniteScroll() {
  window.addEventListener('scroll', handleScroll)
}
```

**[СКРІН 3: Форма пошуку з введеним запитом]**

\newpage

# 4 РОБОТА З МАСИВАМИ ТА ОБ'ЄКТАМИ В JavaScript

В Photo Gallery активно використовуються методи масивів для обробки колекцій фотографій. forEach() для ітерації та створення карток, find() для пошуку конкретної фотографії за ID, some() для перевірки наявності в улюблених, filter() для фільтрації за лайками.

Spread оператор використовується для об'єднання масивів при додаванні нових фотографій у режимі infinite scroll.

Метод forEach для відображення фото:

```javascript
photos.forEach(photo => {
  const photoCard = createPhotoCard(photo)
  elements.photoGallery.appendChild(photoCard)
})
```

Метод find для пошуку фото:

```javascript
export function showPhotoModal(photoId) {
  let photo = state.photos.find(p => p.id === photoId)

  if (!photo) {
    const favorites = getFavorites()
    photo = favorites.find(p => p.id === photoId)
  }

  if (!photo) return

  elements.modalPhotoImg.src = photo.urls.regular
  elements.modalPhotoTitle.textContent =
    photo.alt_description || 'Фото'
  elements.photoModal.show()
}
```

Метод some для перевірки улюбленого:

```javascript
export function isFavorite(photoId) {
  const favorites = getFavorites()
  return favorites.some(fav => fav.id === photoId)
}
```

Методи findIndex і splice для видалення:

```javascript
export function toggleFavorite(photo) {
  const favorites = getFavorites()
  const index = favorites.findIndex(fav => fav.id === photo.id)

  if (index > -1) {
    favorites.splice(index, 1)
  } else {
    favorites.push(photo)
  }

  saveFavorites(favorites)
}
```

Spread оператор для об'єднання масивів:

```javascript
async function loadMorePhotos() {
  state.currentPage++
  const photos = await fetchPhotos(
    state.currentQuery,
    state.currentPage
  )
  state.photos = [...state.photos, ...photos]
  displayPhotos(photos, true)
}
```

Метод filter для фільтрації:

```javascript
let favorites = getFavorites()

if (state.minLikes > 0) {
  favorites = favorites.filter(
    photo => photo.likes >= state.minLikes
  )
}

displayPhotos(favorites)
```

**[СКРІН 4: Вкладка "Улюблені" з фільтрацією]**

\newpage

# 5 РОЗРОБКА HTML-ФОРМ ТА ВАЛІДАЦІЯ ДАНИХ

В Photo Gallery створено форму пошуку фотографій з валідацією. Форма має текстове поле з атрибутом required для базової HTML5 валідації. Додано додаткову JavaScript валідацію через метод trim() для перевірки порожніх значень.

HTML форма пошуку (index.html):

```html
<form id="searchForm" class="d-flex gap-2">
  <input
    type="text"
    id="searchInput"
    class="form-control"
    placeholder="Введіть ключове слово..."
    required
  />
  <button type="submit" class="btn btn-primary">
    Пошук
  </button>
</form>
```

JavaScript валідація:

```javascript
const query = elements.searchInput.value.trim()

if (!query) {
  showError('Будь ласка, введіть пошуковий запит')
  return
}
```

Форма фільтрації з number input:

```html
<div class="col-md-6">
  <label for="minLikes" class="form-label small">
    Мінімум лайків:
  </label>
  <input type="number" id="minLikes"
         class="form-control form-control-sm"
         placeholder="Наприклад: 100"
         min="0" step="10">
</div>
```

Обробка фільтра з debouncing:

```javascript
let likesTimeout

export function setupLikesFilter() {
  elements.minLikesInput.addEventListener('input', () => {
    clearTimeout(likesTimeout)

    likesTimeout = setTimeout(() => {
      const value = parseInt(
        elements.minLikesInput.value
      ) || 0
      state.minLikes = value

      if (state.currentTab === 'favorites') {
        let favorites = getFavorites()
        if (state.minLikes > 0) {
          favorites = favorites.filter(
            photo => photo.likes >= state.minLikes
          )
        }
        displayPhotos(favorites)
      }
    }, 500)
  })
}
```

**[СКРІН 5: Повідомлення про помилку валідації]**

\newpage

# 6 ВИКОРИСТАННЯ BOOTSTRAP ТА AXIOS: АДАПТИВНИЙ ІНТЕРФЕЙС І ПЕРШІ ЗАПИТИ ДО API

В Photo Gallery інтегровано Bootstrap 5.3.8 для створення адаптивного інтерфейсу. Використано компоненти: Navbar, Card, Modal, Tabs, Grid system.

Axios використовується для HTTP запитів до Unsplash API з автоматичним парсингом JSON.

Імпорт Bootstrap та Axios (src/main.js):

```javascript
import './style.css'
import 'bootstrap/dist/css/bootstrap.min.css'
import * as bootstrap from 'bootstrap'
import axios from 'axios'
```

Bootstrap Navbar (index.html):

```html
<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1">
      📸 Photo Gallery
    </span>
  </div>
</nav>
```

Bootstrap Modal:

```html
<div class="modal fade" id="photoModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered modal-xl">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalPhotoTitle">
          Фото
        </h5>
        <button type="button" class="btn-close"
                data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body text-center">
        <img id="modalPhotoImg" src=""
             class="img-fluid" />
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary"
                data-bs-dismiss="modal">Закрити</button>
        <a id="downloadPhotoBtn" href="" target="_blank"
           class="btn btn-primary">Відкрити оригінал</a>
      </div>
    </div>
  </div>
</div>
```

Bootstrap Grid для галереї:

```html
<div id="photoGallery"
     class="row row-cols-1 row-cols-md-3 g-4">
</div>
```

Axios для HTTP запитів (src/api.js):

```javascript
export async function fetchPhotos(query = 'nature', page = 1) {
  try {
    const params = {
      query: query,
      page: page,
      per_page: 12,
      client_id: UNSPLASH_ACCESS_KEY
    }

    const response = await axios.get(
      `${UNSPLASH_API_URL}/search/photos`,
      { params }
    )

    return response.data.results
  } catch (error) {
    // Обробка помилок
  }
}
```

**[СКРІН 6: Адаптивний дизайн на різних екранах]**

\newpage

# 7 ВЗАЄМОДІЯ З ЗОВНІШНІМИ API: HTTP-ЗАПИТИ, FETCH/AXIOS ТА ОБРОБКА JSON

Photo Gallery інтегровано з Unsplash API для отримання фотографій. Реалізовано асинхронну функцію fetchPhotos() яка відправляє GET запити до endpoint /search/photos.

Конфігурація API (src/config.js):

```javascript
export const UNSPLASH_ACCESS_KEY =
  'EyuRTN2cUCiVfSzRT9Us4oc-kHgYsSuqrFrzqvNDumk'
export const UNSPLASH_API_URL =
  'https://api.unsplash.com'
```

Асинхронна функція запиту (src/api.js):

```javascript
import axios from 'axios'
import { UNSPLASH_ACCESS_KEY, UNSPLASH_API_URL }
  from './config.js'
import { state } from './state.js'
import { showLoading, hideError, showError }
  from './ui.js'

export async function fetchPhotos(
  query = 'nature',
  page = 1
) {
  try {
    state.isLoading = true
    showLoading(true)
    hideError()

    const params = {
      query: query,
      page: page,
      per_page: 12,
      client_id: UNSPLASH_ACCESS_KEY
    }

    if (state.sortOrder !== 'relevant') {
      params.order_by = state.sortOrder
    }

    const response = await axios.get(
      `${UNSPLASH_API_URL}/search/photos`,
      { params }
    )

    state.isLoading = false
    showLoading(false)

    return response.data.results
  } catch (error) {
    state.isLoading = false
    showLoading(false)
    handleError(error)
    return []
  }
}
```

Структура JSON відповіді:

```json
{
  "results": [
    {
      "id": "photo-id",
      "urls": {
        "small": "https://...",
        "regular": "https://..."
      },
      "user": {
        "name": "Photographer Name",
        "username": "username"
      },
      "likes": 123,
      "alt_description": "Description"
    }
  ]
}
```

**[СКРІН 7: Результати отримані з Unsplash API]**

\newpage

# 8 ОБРОБКА ПОМИЛОК У JAVASCRIPT ТА ПІД ЧАС МЕРЕЖЕВИХ ЗАПИТІВ

В Photo Gallery реалізовано багаторівневу систему обробки помилок. Всі асинхронні операції обгорнуто в try-catch блоки. Розрізняються типи помилок від Axios.

Try-catch для API запитів:

```javascript
export async function fetchPhotos(
  query = 'nature',
  page = 1
) {
  try {
    state.isLoading = true
    showLoading(true)

    const response = await axios.get(
      `${UNSPLASH_API_URL}/search/photos`,
      {
        params: {
          query, page, per_page: 12,
          client_id: UNSPLASH_ACCESS_KEY
        }
      }
    )

    state.isLoading = false
    showLoading(false)

    return response.data.results
  } catch (error) {
    state.isLoading = false
    showLoading(false)

    if (error.response) {
      if (error.response.status === 401) {
        showError('Невірний API ключ')
      } else if (error.response.status === 403) {
        showError('Перевищено ліміт запитів')
      } else {
        showError(`Помилка API: ${error.response.status}`)
      }
    } else if (error.request) {
      showError('Помилка мережі')
    } else {
      showError('Невідома помилка: ' + error.message)
    }

    return []
  }
}
```

Обробка помилок localStorage:

```javascript
export function getFavorites() {
  try {
    const favorites = localStorage.getItem('favorites')
    return favorites ? JSON.parse(favorites) : []
  } catch (error) {
    console.error('Помилка читання:', error)
    return []
  }
}

export function saveFavorites(favorites) {
  try {
    localStorage.setItem(
      'favorites',
      JSON.stringify(favorites)
    )
    updateFavoritesCount()
  } catch (error) {
    console.error('Помилка запису:', error)
    showError('Не вдалося зберегти')
  }
}
```

Відображення помилок (src/ui.js):

```javascript
export function showError(message) {
  elements.errorAlert.textContent = message
  elements.errorAlert.classList.remove('d-none')
}

export function hideError() {
  elements.errorAlert.classList.add('d-none')
}
```

**[СКРІН 8: Повідомлення про помилку мережі]**

\newpage

# 9 ПАГІНАЦІЯ ТА НЕСКІНЧЕННИЙ СКРОЛ У WEB-ЗАСТОСУНКАХ

В Photo Gallery реалізовано три режими завантаження: класична пагінація, кнопка "Завантажити більше" та infinite scroll.

Функція завантаження додаткових фото:

```javascript
export async function loadMorePhotos() {
  if (state.isLoading || state.currentTab !== 'all')
    return
  if (state.loadingMode === 'pagination')
    return

  state.currentPage++
  const photos = await fetchPhotos(
    state.currentQuery,
    state.currentPage
  )
  state.photos = [...state.photos, ...photos]
  displayPhotos(photos, true)
}
```

Infinite scroll з debouncing:

```javascript
function isBottomReached() {
  const scrollTop = window.pageYOffset ||
    document.documentElement.scrollTop
  const scrollHeight =
    document.documentElement.scrollHeight
  const clientHeight =
    document.documentElement.clientHeight

  return scrollTop + clientHeight >=
    scrollHeight - 300
}

let scrollTimeout

function handleScroll() {
  clearTimeout(scrollTimeout)

  scrollTimeout = setTimeout(async () => {
    if (isBottomReached() &&
        state.currentTab === 'all' &&
        !state.isLoading &&
        state.loadingMode === 'infinite') {
      await loadMorePhotos()
    }
  }, 200)
}

export function setupInfiniteScroll() {
  window.addEventListener('scroll', handleScroll)
}
```

Кнопка "Завантажити більше":

```javascript
export function setupLoadMoreButton() {
  elements.loadMoreBtn.addEventListener(
    'click',
    async () => {
      await loadMorePhotos()
    }
  )
}
```

**[СКРІН 9: Infinite scroll з автозавантаженням]**

\newpage

# 10 ВИКОРИСТАННЯ LOCALSTORAGE ТА КЕШУВАННЯ ДАНИХ НА КЛІЄНТІ

В Photo Gallery реалізовано систему улюблених фотографій через localStorage. Масив об'єктів перетворюється у JSON через JSON.stringify() при збереженні та парситься через JSON.parse() при читанні.

Збереження даних (src/storage.js):

```javascript
export function saveFavorites(favorites) {
  try {
    localStorage.setItem(
      'favorites',
      JSON.stringify(favorites)
    )
    updateFavoritesCount()
  } catch (error) {
    console.error('Помилка запису:', error)
    showError('Не вдалося зберегти')
  }
}
```

Читання даних:

```javascript
export function getFavorites() {
  try {
    const favorites = localStorage.getItem('favorites')
    return favorites ? JSON.parse(favorites) : []
  } catch (error) {
    console.error('Помилка читання:', error)
    return []
  }
}
```

Додавання/видалення:

```javascript
export function toggleFavorite(photo) {
  const favorites = getFavorites()
  const index = favorites.findIndex(
    fav => fav.id === photo.id
  )

  if (index > -1) {
    favorites.splice(index, 1)
  } else {
    favorites.push(photo)
  }

  saveFavorites(favorites)

  if (state.currentTab === 'favorites') {
    displayPhotos(favorites)
  }
}
```

Оновлення лічильника:

```javascript
export function updateFavoritesCount() {
  const favorites = getFavorites()
  elements.favoritesCount.textContent = favorites.length
}
```

**[СКРІН 10: localStorage в Chrome DevTools]**

\newpage

# 11 ІНСТРУМЕНТИ НАЛАГОДЖЕННЯ JAVASCRIPT-КОДУ

При розробці Photo Gallery використовувалися інструменти налагодження для виявлення та виправлення помилок.

Console для виводу інформації:

```javascript
console.log('Завантажено фото:', photos.length)
console.error('Помилка API:', error)
console.warn('Перевищено ліміт запитів')
```

Використання Chrome DevTools:

- **Elements** - перевірка DOM структури
- **Console** - перегляд помилок та логів
- **Network** - моніторинг HTTP запитів
- **Application** - перевірка localStorage

Обробка помилок з детальними повідомленнями:

```javascript
try {
  const data = await fetchPhotos(query)
} catch (error) {
  if (error.response) {
    console.error('Помилка сервера:',
      error.response.status)
  } else if (error.request) {
    console.error('Помилка мережі')
  } else {
    console.error('Помилка:', error.message)
  }
}
```

**[СКРІН 11: Chrome DevTools з Network вкладкою]**

\newpage

# 12 ПІДГОТОВКА WEB-ЗАСТОСУНКУ ДО ПУБЛІКАЦІЇ ТА РОЗГОРТАННЯ

Для підготовки застосунку до публікації виконано оптимізацію та збірку.

Команда для збірки production версії:

```bash
npm run build
```

Структура збірки:

```
dist/
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── [image files]
└── index.html
```

Оптимізації Vite:

- Мініфікація JavaScript та CSS
- Tree-shaking невикористаного коду
- Оптимізація зображень
- Code splitting для швидшого завантаження

Конфігурація .gitignore:

```
node_modules
dist
dist-ssr
*.local
.DS_Store
```

Попередній перегляд production збірки:

```bash
npm run preview
```

Deployment опції:

- **GitHub Pages** - безкоштовний хостинг
- **Netlify** - автоматичний deployment
- **Vercel** - швидкий хостинг для Vite

**[СКРІН 12: Збірка проєкту]**

\newpage

# 13 ПРЕЗЕНТАЦІЯ ПРОЄКТУ

Проєкт Photo Gallery демонструє повний цикл розробки сучасного веб-застосунку з використанням актуальних технологій.

## Основні досягнення:

✅ **Модульна архітектура** - розділення на config, state, dom, api, ui, events, storage модулі

✅ **Інтеграція з API** - повна взаємодія з Unsplash API

✅ **Адаптивний дизайн** - Bootstrap Grid та медіа-запити

✅ **Обробка помилок** - комплексна система try-catch

✅ **Три режими завантаження** - пагінація, кнопка, infinite scroll

✅ **LocalStorage** - збереження улюблених фотографій

✅ **Інтерактивність** - модальні вікна, фільтри, категорії

## Технічний стек:

- Vite 7.2.4 - збірка модулів
- Bootstrap 5.3.8 - UI фреймворк
- Axios 1.13.2 - HTTP клієнт
- JavaScript ES6+ - мова програмування
- Git - система контролю версій

## Функціонал:

- Пошук фотографій за ключовими словами
- Швидкий доступ до категорій
- Сортування за популярністю/новизною
- Збереження улюблених з фільтрацією
- Перегляд у повному розмірі

**[СКРІН 13: Головна сторінка Photo Gallery]**

**[СКРІН 14: Вкладка улюблених]**

\newpage

# ВИСНОВКИ

В результаті виконання навчальної практики з розробки WEB-застосунків було створено повнофункціональний веб-застосунок **Photo Gallery** з використанням сучасних технологій та інструментів.

## Досягнуті результати

**Всі 12 тем практики повністю реалізовані:**

1. ✅ Створення та налаштування WEB-проєкту - Vite, Git, модульна структура
2. ✅ Робота з DOM-деревом - createElement, appendChild, classList
3. ✅ Обробка подій користувача - submit, click, scroll, делегування, debouncing
4. ✅ Робота з масивами та об'єктами - forEach, find, some, filter, spread
5. ✅ Розробка HTML-форм та валідація - required, JavaScript валідація
6. ✅ Використання Bootstrap та Axios - Navbar, Modal, Tabs, HTTP запити
7. ✅ Взаємодія з зовнішніми API - Unsplash API, JSON, async/await
8. ✅ Обробка помилок у JavaScript - try-catch, типи помилок
9. ✅ Пагінація та нескінченний скрол - три режими завантаження
10. ✅ Використання localStorage - JSON.stringify/parse, збереження даних
11. ✅ Інструменти налагодження - Chrome DevTools, Console
12. ✅ Підготовка до публікації - оптимізація, збірка, deployment

## Набуті навички

**Технічні:** Vite, Axios, Bootstrap 5, Unsplash API, Git, Chrome DevTools, ES6+ модулі, async/await, деструктуризація, spread оператор.

**Методологічні:** модульна архітектура, асинхронне програмування, обробка помилок, оптимізація (debouncing), event delegation, immutable підхід.

**UX/UI:** інтуїтивний інтерфейс, вибір режимів завантаження, візуальний feedback, адаптивний дизайн.

## Практична цінність

Застосунок демонструє реальну інтеграцію з зовнішнім API, сучасні підходи до веб-розробки, best practices JavaScript та готовність до deployment.

Проєкт повністю відповідає методичним рекомендаціям та охоплює всі обов'язкові теми з детальними прикладами коду.

\newpage

# ПЕРЕЛІК ВИКОРИСТАНИХ ДЖЕРЕЛ

## Офіційна документація

1. **Unsplash API Documentation**
   https://unsplash.com/documentation

2. **Vite Documentation**
   https://vitejs.dev/guide/

3. **Bootstrap 5 Documentation**
   https://getbootstrap.com/docs/5.3/

4. **Axios Documentation**
   https://axios-http.com/docs/intro

5. **MDN Web Docs - JavaScript**
   https://developer.mozilla.org/en-US/docs/Web/JavaScript

6. **MDN Web Docs - HTML**
   https://developer.mozilla.org/en-US/docs/Web/HTML

7. **MDN Web Docs - CSS**
   https://developer.mozilla.org/en-US/docs/Web/CSS

8. **MDN Web Docs - Web Storage API**
   https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API

## Навчальні матеріали

9. **JavaScript.info** - Сучасний підручник JavaScript
   https://uk.javascript.info/

10. **W3Schools - JavaScript Tutorial**
    https://www.w3schools.com/js/

## Методичні рекомендації

11. **Навчальна практика з розробки WEB-застосунків**
    Методичні рекомендації щодо виконання практичних завдань
    Олександрійський політехнічний фаховий коледж, 2025

## Інструменти розробки

12. **Git Documentation**
    https://git-scm.com/doc

13. **NPM Documentation**
    https://docs.npmjs.com/

14. **Chrome DevTools**
    https://developer.chrome.com/docs/devtools/

15. **Visual Studio Code**
    https://code.visualstudio.com/docs
